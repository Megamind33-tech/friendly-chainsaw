{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "control-room",
  "description": "Capability for document/project persistence, scoped to the Control Room window only",
  "windows": ["control-room"],
  "permissions": [
    "sql:default",
    "sql:allow-execute"
  ]
}
