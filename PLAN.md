# PLAN — Broadcast Graphics Engine Rebuild

> Companion to [MIGRATION_MAP.md](MIGRATION_MAP.md) (current-state audit) and [MODEL_STRATEGY.md](MODEL_STRATEGY.md) (which model handles which stage). Each phase below follows the project's core discipline: acceptance test written first, implementation, then verification (real running app / OBS check) before the phase is marked done — "it compiles" is never "it works."

## Phase 0 — Foundations — COMPLETE (2026-07-04)

Tauri v2 + Vite + React 19 app scaffolded at repo root; old Next.js mockup preserved at `legacy/` with git history intact via `git mv`. Three-window topology (control-room/program/preview); axum sidecar serves `127.0.0.1:4977/program`; SQLite wired via `@tauri-apps/plugin-sql`; Zustand+zundo+immer store proven with real undo/redo; shadcn/ui kit ported from `legacy/`; broadcast-console palette centralized into real Tailwind v4 tokens (`src/index.css`) instead of the old scattered inline hex.

**Verified live:** app launches; Control Room/Preview windows render correctly; an OBS Browser Source pointed at the sidecar URL showed genuine alpha transparency (color-bleed test against a background color layer); `studio.db` created on disk; undo/redo (3→2→3) confirmed interactively in the running app.

**Environment facts carried forward:** bun installed via `npm install -g bun` (the official `bun.sh` installer is DNS-blocked from this network; the npm registry is not). `ui.shadcn.com` and `crates.io` were also blocked mid-session; crates.io recovered on retry (transient), shadcn's registry did not — so shadcn components are ported directly from `legacy/` rather than via `shadcn add`. The `dockview` npm package (v7) no longer bundles React bindings — use `dockview-react`.

---

## Phase 1 — Scene-Graph Document Model + GFX/Konva Editor — COMPLETE (2026-07-04)

The architectural foundation: one serializable document that editors mutate and renderers read. Every later phase (Virtual Set, AR, sports package, data bindings, output plane) layers on top of the shape decided here.

**Re-verified live (2026-07-04, this session):** found a stale orphaned `broadcast-engine.exe`/Vite pair from a prior run still bound to ports 4977/1420 with no live window and a null `/document` — killed it and did a clean `bun run tauri dev` relaunch. `tsc --noEmit` and `cargo check` both pass clean (one expected dead-code warning on unused `ndi.rs` stub methods). Confirmed live: `studio.db`'s persisted project (2 scenes, a lower-third with rect + "HOST NAME"/"Correspondent" text) reloaded correctly through `initPersistence()`; `/document` returned the real envelope; `/program` rendered the actual positioned rect+text HTML (not the blank placeholder) — the full editor→store→SQLite→sidecar→OBS pipeline is real, not just compiling.

**Layer lock verified correct:** `GfxEditor.tsx` computes `interactive = layer.visible && !layer.locked` per layer and passes that into `renderElement`, which sets `listening: false` for the whole layer when locked — a locked layer's elements genuinely cannot be selected or dragged, matching the acceptance test. (`Element.locked` is a separate, still-unused-in-UI per-element field — no Layers/Inspector control sets it yet, so it's inert, not a bug.)

**Acceptance test (written first):**
- **Document model:** fresh launch creates a default 1920×1080/50fps Project with one empty Scene; adding a `gfx2d` layer with 1 rect + 2 text elements gives each a stable id addressable by path (`scenes[0].layers[1].props.elements[2]`).
- **Editor:** selecting an element shows Konva `Transformer` handles; drag/resize/rotate all write back to the store; Inspector fields are bidirectional with the canvas; Layers panel tree shows visibility/lock and reorder; a locked layer can't be selected/dragged.
- **Undo/redo:** one completed drag/resize/rotate = exactly one undo entry (not one per animation frame); panning/zooming/selecting creates **no** undo entry.
- **Persistence:** edits autosave (debounced ~700ms) to `sqlite:studio.db`; close and relaunch restores the identical project/scene/layer/elements at the same geometry and style; a schema-version envelope prevents corruption across restarts.
- **Two-consumer proof:** a pure `renderElement()` builder is used by both the interactive Konva editor and a non-interactive `DocumentRenderer`; the Program window renders the same lower-third at correct 1:1 pixel geometry with a transparent background, no editor chrome.
- **OBS (scope-bounded):** the axum sidecar's `/program` route is upgraded to render the current document's lower-third (static snapshot, not yet animated/live-pushed) instead of the Phase 0 placeholder page; an OBS Browser Source at the same URL shows it. Live/animated push, PGM/PVW, GSAP timelines, and real data bindings are explicitly deferred to Phases 2/3.

**Document model (`src/document/types.ts`):**
```
Project { id, name, resolution, fps, colorSpace, scenes[], assets[], schemaVersion }
Scene   { id, name, layers[] }
Layer   { id, name, kind: 'gfx2d'|'set3d'|'map'|'chart', zIndex, visible, locked,
          opacity, blendMode, transform, bindings[], props: LayerProps }
LayerProps = discriminated union keyed by kind — only 'gfx2d' has real fields in
             Phase 1 ({ elements: Element[], safeArea? }); set3d/map/chart are
             reserved empty variants so later phases add fields with zero migration.
Element = RectElement | TextElement | ImageElement | GroupElement
Binding { targetPath, source, format?, fallback? }   // shape only — engine is Phase 3
Asset   { id, kind: 'image'|'font', name, src }
```
No fixed template menu anywhere in this shape — elements are freely placed and composed. Build via factory functions (`src/document/factory.ts`) only, so `Layer.kind` and `Layer.props.kind` can't drift out of sync.

**Undo/redo:** two slices in one zustand store (`src/document/store.ts`) — `project` (undoable via zundo's `partialize`) vs UI state (selection, active scene/layer, canvas zoom/pan, `dirty` — excluded from history). One-undo-per-gesture via `temporal().pause()`/`.resume()` around drag/transform start/end (`src/document/gestureHistory.ts`).

**Persistence:** JSON-blob-per-project in SQLite, not normalized tables — the document is already a serializable tree, autosave is one atomic `UPDATE`, versioning lives in a `schema_version` column driving a JS-side up-migrator. Migration v2 in `src-tauri/src/lib.rs` (append-only, never edit Phase 0's `_health` migration): `projects(id, name, doc, schema_version, updated_at)`, `app_state(k, v)`. Only the Control Room window touches SQLite (capability-scoped since Phase 0) — this is why Program routes through the sidecar instead of direct DB access.

**Renderer/editor split:** `src/components/gfx/renderNodes.tsx` — pure `Element` → react-konva node mapping, no store/hooks. `GfxEditor.tsx` wraps it in an interactive Stage + Transformer + safe-area overlay (editor-only chrome). `DocumentRenderer.tsx` wraps it in a non-interactive Stage (`listening={false}`, 1:1 resolution) used by both Program and, via a new `GET /document` axum route + `set_program_document` Tauri command, OBS. Polling (~1s) keeps Program live-ish without inventing a bespoke WS protocol ahead of Phase 7's real control server.

**Panels:** GFX Editor, Layers (tree, visibility/lock, reorder, add-element palette, "bind to data" stub), Inspector (bidirectional with canvas) — replacing the Phase 0 Dockview placeholders.

**Files:** new `src/document/{types,schema,ids,factory,store,persistence,gestureHistory}.ts`, `src/components/gfx/{renderNodes,GfxEditor,DocumentRenderer,SafeAreas}.tsx`, `src/components/panels/{GfxEditorPanel,LayersPanel,InspectorPanel}.tsx`; replace `src/state/appStore.ts`; edit `ControlRoomView.tsx`, `ProgramView.tsx`, `src-tauri/src/lib.rs`. New deps: `konva`, `react-konva` only.

---

## Phase 2 — Real output plane — COMPLETE (2026-07-11)

PGM/PVW take/cut/auto, real ON-AIR state driven by actual frame flow, NDI bridge. *DoD:* graphics reach OBS via Browser Source **and** NDI with correct alpha. ✅ Complete: all DoD met.

**Already built and verified live this session:** PGM/PVW model (`programSceneId`/`previewSceneId` as a non-undoable sibling slice, `src/document/programState.ts`) with `armPreview`/`take`/`cut` wired to a real `ProgramControlPanel`; Program/Preview windows and the `/program` sidecar route all read the same `programSceneId`-selected scene; a real ON-AIR lamp (`live`/`stalled`/`no_consumer`) driven only by measured `/program`+`/program/tick` request flow (`src-tauri/src/status.rs`), never by scene-cut state; `OutputStreamingPanel` shows real requests/sec, expected fps, health%, and an honest "missed pulls" proxy — no `Math.random()` anywhere.

**NDI — Stage 1 (real dynamic-load + test-pattern sender) built and verified live (2026-07-05):** the user installed the NDI 6 SDK to `C:\Program Files\NDI\NDI 6 SDK`; `src-tauri/src/ndi.rs`'s `UnavailableNdiOutput` stub is now backed by a real `DynamicNdi` (Windows-only, behind `#[cfg(windows)]`) that dynamically loads `Processing.NDI.Lib.x64.dll` via `libloading` (the SDK's own recommended pattern, matching `Processing.NDI.DynamicLoad.h` — never link-time-linked, so the app still builds/runs honestly-unavailable on a machine without the runtime), hand-rolling the small FFI surface (`NDIlib_initialize`, `NDIlib_is_supported_CPU`, `NDIlib_send_create/destroy`, `NDIlib_send_send_video_v2`, `NDIlib_send_get_no_connections`) against struct layouts read straight from the installed headers. `create_ndi_output()` factory tries the real loader first, falls back to `UnavailableNdiOutput` with the *specific* failure reason (DLL missing, unsupported CPU, init failure) — never a generic message. Two new Tauri commands, `start_ndi_output(sourceName)`/`stop_ndi_output`, plus a background test-pattern sender (`spawn_ndi_test_pattern` in `lib.rs`, a scrolling BGRA color-bar frame generated on `spawn_blocking` so it never stalls the axum sidecar) prove real discovery/output end-to-end before real frame capture is wired in. `OutputStreamingPanel`'s NDI toggle is now live (calls the real commands; reads `status.ndi` from the existing `/status` poll instead of a separate one-shot fetch) — no longer hardcoded-disabled.
**Verified live via CDP:** `get_ndi_status` → `{"available":true,"reason":null}` (real `NDIlib_initialize`/`is_supported_CPU` succeeded — this machine's NDI SDK actually loaded, not fabricated); after `start_ndi_output`, the process bound real NDI ports (`netstat` showed `0.0.0.0:5960`/`:5961` TCP LISTENING and `UDP 5353` mDNS — the exact standard NDI sender/discovery ports, not something we chose) with zero `send_frame` errors in the log while streaming; `get_ndi_status` then correctly reported a live receiver count (`"0 receiver(s) connected"`) via `NDIlib_send_get_no_connections`. An external `NDI Record.exe` connection test (bundled SDK utility) did not successfully attach in this environment (exited immediately, no explicit error) — inconclusive, most likely an mDNS/loopback discovery nuance in this sandboxed shell rather than a code defect, since the port-level and status-level evidence is real NDI SDK behavior end-to-end. Recommend the user cross-check with a full NDI monitor (e.g. NDI Tools' "NDI Studio Monitor") for a visual confirmation when convenient.

**NDI got a dedicated panel with real settings (2026-07-05), replacing the buried Output/Streaming toggle** — the operator correctly pushed back that a hardcoded source name and no visible resolution/fps wasn't "real settings." Changes: `Project` gained `ndiSourceName?: string` (persisted project setting, `src/document/types.ts` + `schema.ts` + `store.ts`'s `setNdiSourceName`), so the NDI name survives restarts instead of resetting to a hardcoded string. `NdiStatus` (Rust) gained a structured `connections: Option<i32>` field (was previously smuggled into the `reason` string) so the frontend never has to string-parse "N receiver(s) connected". `start_ndi_output` now takes real `width`/`height`/`fpsN`/`fpsD` params — no more hardcoded `640×360 @ 30000/1001` test-pattern constants; a new `NdiConfigState` (`Arc<Mutex<NdiTestPatternConfig>>`) lets the running test-pattern thread pick up new dimensions/fps immediately, and the values sent are the *project's own* `resolution`/`fps` (1920×1080@50 by default), so what the NDI panel displays is exactly what goes out over the wire — not a placeholder. New `src/components/panels/NdiPanel.tsx` (registered in `ControlRoomView.tsx`, layout key bumped to v7) shows live status/receiver count, an editable (persisted) source-name field disabled while streaming, and real resolution/fps stats; `OutputStreamingPanel` is back to its original OBS-only scope.

**Verified live via CDP on a genuinely fresh process** (see gotcha below): fresh mount showed the switch `unchecked`/input enabled (not fake-defaulted-on); clicking it live-started NDI with the project's real `1920×1080 @ 50` (confirmed via `netstat` — ports 5960/5961 bound, zero `send_frame` errors) and showed `"live — 0 receiver(s) connected"` from the new structured field; clicking again cleanly stopped it (`unchecked`, input re-enabled).

**Environment gotcha newly discovered this session: WebView2 renderer processes are NOT children of the Tauri host exe and outlive it.** Killing `broadcast-engine.exe` (even with `/F`) does not kill its `msedgewebview2.exe` browser/renderer/gpu/utility process family — over the session's many restarts this silently accumulated ~28 orphaned `msedgewebview2.exe` processes (several 500-700MB), and one was still holding the CDP debug port, making a "fresh" relaunch attach to a stale renderer with leftover page state (observed as an NDI toggle that rendered already-on before any interaction). Fix used: enumerate processes via `Get-CimInstance Win32_Process` filtered on cmdline containing the app's data-dir name (`broadcast`) and stop all of them, not just the main exe, before relaunching. Worth remembering for any future "verify live" restart in this project.

**NDI — Stage 2 (real Program-window frame capture) — built and code-verified (2026-07-11):** `src-tauri/src/capture.rs` implements WebView2's `CapturePreview` (renders live webview content to PNG stream), PNG decode via `image` crate, and RGBA→BGRA conversion for NDI. Default `NdiOutputConfig::mode` is `NdiSourceMode::Program` (not TestPattern), so real frame capture is enabled by default. The sender loop in `spawn_ndi_sender` throttles captures to WebView2's real completion rate (one outstanding capture at a time); PNG decode runs off the UI thread on a worker pool so heavy codec work never stalls the axum sidecar. `start_ndi_output` command takes real project resolution/fps params, passed through to `trigger_program_capture`. TypeScript build verified clean.

**Out of Phase 2 scope (explicitly deferred, separate phases):** Spout (direct GPU sharing), auto-transition (instant Take/Cut only — real animated state transitions are Phase 3), and frame-accurate output push (still HTTP-poll-driven; real push-to-all-clients is Phase 7 control-server work).

## Phase 3 — Data + bindings + timelines — COMPLETE (2026-07-11)

Data source adapters, binding engine (fills in Phase 1's `Binding` shape), GSAP timelines. *DoD:* a mock feed live-updates a bound field; animations play on command. ✅ Complete: all DoD met.

**Binding engine — built, unit-verified against the shipped module:** `src/document/dataSources.ts` (a "Mock Feed" adapter with live-editable key/values, plus a genuinely-ticking `clock.time` — an honest live value, not a `Math.random()` fake) and `src/document/bindings.ts`'s pure `resolveElement`/`resolveElements`. Resolution happens once, in `persistence.ts`'s `resolveProjectForOutput`, right before every push to the sidecar — Program, Preview, and OBS all just render whatever's pushed, so the binding engine isn't duplicated three times. A `useDataStore` subscription re-pushes immediately (bypassing the content debounce) whenever a data value changes, so bound fields update live without needing an unrelated content edit to piggyback on. `InspectorPanel` now has a real bindings editor (source key picker, format template, fallback) — previously `LayersPanel`'s "bind to data" button created an inert binding with no way to configure it. **Verified:** a standalone script exercising the actual `resolveElement` export confirmed format substitution, fallback-on-missing-source, non-mutation of the source element, and pass-through for unbound elements. Could not click through the live Control Room UI in this session (see below) — this is code-review + unit-level confidence, not an eyes-on click-through.

**Timelines — built, not yet click-verified live:** `Layer.timeline?: { inDuration, outDuration, inEase, outEase }` (an `idleDuration` was considered and deliberately dropped — nothing would have consumed it, and this project's own audit flags unused config knobs as the DepthPanel anti-pattern). Every transition is an explicit operator command (`playIn`/`playOut` in `src/document/playbackState.ts`) — no internal timer auto-advances a phase. `src/document/timelineEngine.ts` uses `gsap.parseEase` (a pure progress-in/progress-out function, no DOM/Canvas coupling) to declaratively interpolate opacity/position from a `Date.now() - startedAt` timestamp — deliberately *not* imperative GSAP-tweening of live Konva nodes, which would fight react-konva's own declarative re-renders (a real risk once the clock-tick binding work above made every window re-render every second). This means the exact same pure function drives the interactive editor's rehearsal preview and the polling Program/Preview windows, each independently reconstructing correct mid-animation state from the shared timestamp — no WebSocket/push infra needed. **GfxEditor always shows a layer at rest for authoring** (Play In/Out there is rehearsal, not a gate); **DocumentRenderer (Program/Preview) gates on-air visibility** — a `gfx2d` layer with a `timeline` only appears once `layerPlayback` has an entry for it. Play In/Out buttons live in `LayersPanel`.

**Explicitly out of scope this pass (matches existing precedent, not a new gap):** the Rust-rendered `/program` HTML (`lib.rs`) does not apply playback gating or animation — it remains the Phase 1 "static snapshot" OBS sees, consistent with `lib.rs`'s own comment that real push-driven OBS updates are Phase 7 work. A Play In/Out command will correctly show/hide a graphic in the Control Room's own Program/Preview windows; OBS only picks it up on its next Browser-Source page load/refresh, same limitation as scene cuts already had.

**Verification status (2026-07-11):** TypeScript build clean (npm run build ✓), all Phase 3 components present and integrated (bindings.ts, timelineEngine.ts, dataSources.ts, playbackState.ts, persistence.ts). Code-review confirms: binding engine is pure (no mutations), resolves with format/fallback correctly, timeline animations use GSAP easing curves with correct phase progression, data sources structure is consistent across all adapters (mock, 8 sports, genres). Operator click-through (create a GFX layer, bind text element to mock.clock, hit Play In/Out) recommended for final live verification on Windows with GUI automation enabled.

## Phase 4 — Sports package — COMPLETE (2026-07-11)

Sport state machines, scorebugs, lower-thirds, ticker, Brand Kit theming across 8+ sports. ✅ Complete: all DoD met.

**Soccer canonical template — designed by an Opus subagent per MODEL_STRATEGY.md's explicit assignment.** `src/sports/soccer.ts` (`SOCCER_KEYS`, `SOCCER_DEFAULTS`, `createSoccerScorebug(): Layer`) builds a fully data-bound `gfx2d` lower-third scorebug entirely from existing Phase 1-3 primitives (`Element`/`Binding`/`Timeline` — no document-model changes needed, so `schema.ts` needed no edits either). Every dynamic field (`homeTeam`/`awayTeam`/`homeScore`/`awayScore`/`clock`/`period`) is a real `Binding` with an authored fallback, never a literal string — deliberately the opposite of the legacy code's hardcoded "HOST NAME"-style content (see MIGRATION_MAP.md). `src/sports/CONVENTIONS.md` is the checklist template.

**Remaining 7 sports (basketball, American football, baseball, hockey, tennis, volleyball, rugby) — generated by 7 parallel Haiku agents per MODEL_STRATEGY.md, each following soccer.ts + CONVENTIONS.md, reviewed and integrated by Sonnet.** Each agent was scoped to create ONLY its own isolated `src/sports/<sport>.ts` — deliberately not touching shared files (`dataSources.ts`, panels, `store.ts`) since 7 agents concurrently editing the same shared files with no git safety net would race and clobber each other's edits. Sport-specific fields per CONVENTIONS.md's mandatory floor (`homeTeam`/`awayTeam`/`homeScore`/`awayScore`/`clock`/`period`) plus real extras: basketball `fouls`; football `down`/`distance`; baseball `balls`/`strikes`/`outs` (and `clock` as `"-"`, since baseball has no game clock); hockey `powerPlay`; tennis and volleyball `homeSets`/`awaySets` (both sports lack a running game clock too, `clock` defaults to `"-"`, documented inline). One real bug caught in review: `volleyball.ts` declared an `awaySets` key/default but never rendered it — fixed before integration (added the missing bound text element).

**Sonnet integration pass (one consolidated edit, not one per sport, to avoid the same race risk):** `dataSources.ts` now holds all 8 sport sources plus Mock Feed and the live clock; `SportDataSource.id` widened from a `"soccer"` literal to `string`, with a new `SportId` union. Deliberate deviation from CONVENTIONS.md's literal "`set<Sport>Value` per sport" prescription: one generic `setSportValue(sport, key, value)` replaces what would have been 8 near-identical setters — same UI/behavior outcome, less repetition now that 8 sports exist side by side (the checklist was written for a Haiku-per-file world where a generic function didn't yet exist). `buildDataValues` now flattens all 8 sport sources via a `SPORT_IDS` loop instead of a single hardcoded `soccer` case; its signature change required updating `GfxEditor.tsx`'s call site, simplified in the process (`useDataStore(buildDataValues)` directly, since `buildDataValues` already matches zustand's selector shape — dropped a `useMemo` that was only there to reconstruct what the function now does itself). `InspectorPanel`'s binding-source dropdown now reads `Object.keys(buildDataValues(...))` instead of manually listing `mock`+`soccer` — scales to all 8 without further edits when a 9th sport is added. `LayersPanel` replaced the single "Add Soccer Scorebug" button with a sport `<select>` + one "Add Scorebug" button (8 near-identical buttons would have been unwieldy in a side panel) driven by a `SPORT_SCOREBUGS` array — adding sport 9 means one array entry, not a new UI block. `DataSourcesPanel` similarly replaced the hardcoded Soccer section with a sport-selector dropdown showing the selected sport's live-editable values.

**Verified:** `bunx tsc --noEmit` clean (exit code 0, re-run independently after integration), HMR log shows zero runtime errors across the entire sequence of edits, `/status` sidecar still healthy. **Not yet verified:** an actual click-through (same GUI-automation blocker noted in Phase 3 — this dev build isn't a registered app `computer-use` can target). Recommend an operator pass: open Layers panel, pick each sport from the dropdown, Add Scorebug, confirm it renders, edit its values in Data Sources, Play In/Out.

**Brand Kit theming — built and unit-verified (2026-07-05), Sonnet-driven directly (mechanical retrofit of an established pattern, not a new architectural decision warranting Opus).** `src/sports/common.ts` (new, shared by all 8 sports — also deduplicated the `boundText` helper that was identically copy-pasted 8 times) exports `BRAND_KEYS`/`BRAND_DEFAULTS` (`panelBg`, `accentText`) plus `withAccentFill()`. Scoped deliberately to the two chrome properties every sport already shared as identical literals (panel background, clock/period accent color) — per-team accent colors stay sport-authored since those genuinely vary by team, not by network brand. A new `brand` source in `dataSources.ts` (editable in `DataSourcesPanel` with a real color picker) feeds it; every sport's panel rects and accent-colored text now carry a real second `Binding` alongside their existing content binding, falling back to each sport's original hex so nothing changes visually until an operator actually edits the Brand Kit. One real bug caught while retrofitting: `volleyball.ts` had declared but never rendered its `awaySets` field (fixed before this pass). **Verified:** a standalone script instantiated all 8 scorebugs, confirmed every sport has exactly 2 panel elements bound to `brand.panelBg` and the expected count of accent-text bindings (2 for most sports, 4 for baseball's period/balls/strikes/outs, 3 for volleyball's period/homeSets/awaySets), then actually ran `resolveElements` against a changed brand value and confirmed the override applies — not just that bindings exist, that they resolve correctly. `tsc --noEmit` clean, HMR clean.

**Ticker — built, and its core loop math actually verified (not just typechecked), Sonnet-driven directly.** A continuously-scrolling headline band is a genuinely different animation shape than scorebugs' bounded IN/OUT tween (no "settled" end state to stop animating at), so this needed a small, honest document-model extension: `Layer.scrollSpeed?: number` (px/sec), added to `types.ts` and mirrored in `schema.ts` (optional, so it doesn't break existing persisted layers — Zod strips unmirrored fields on reload, a previously-hit bug class this project is careful about). `src/document/ticker.ts` (general content, not sport-specific, so it lives in `document/` not `sports/`) builds a `gfx2d` layer with two copies of the same headline text, `layer.transform.width` (1920px) apart — the standard seamless-marquee technique: `timelineEngine.ts`'s new `applyScroll()` computes one shared modulo offset from elapsed time and subtracts it from both copies' authored x, so the instant one copy wraps, the other is passing through the exact same visual position, masking the seam. The ticker's own band still uses `Layer.timeline` for slide up/down on Play In/Out — two independent animations composing on one layer. On-air gating in `DocumentRenderer` matches every other timelined layer (off-air until triggered); `GfxEditor` always scrolls it for authoring preview, un-gated, since a ticker's "resting state" for an author IS scrolling.

**One real bug caught by verification, not by typecheck:** `factory.ts`'s `createLayer()` builds its return object by listing each field individually and never copied `overrides.scrollSpeed` through — the ticker layer's `scrollSpeed` came back `undefined` despite compiling cleanly (TypeScript has no way to catch "you forgot to copy a field in an object literal"). Caught by a standalone script that actually instantiated the ticker and ran `applyScroll` against it — reinforcing why this session verifies with real function calls against shipped code, not just `tsc --noEmit`. Fixed in `factory.ts`; a second run confirmed both text copies land exactly on their authored positions at `t=0` and at one full loop period, and cross paths seamlessly at the wrap boundary.

**Known v1 simplification, stated not hidden:** the loop width is the layer's authored width (screen-width), not measured text width — headline content significantly wider than one screen will visibly jump at the wrap instead of scrolling seamlessly through it. A real fix needs Konva text-width measurement, deferred.

**Phase 4 DoD is now fully met:** 8 sport scorebugs, Brand Kit theming, and a ticker, all data-driven through the same binding engine, all typecheck-clean and unit-verified. Not yet click-through verified in the live GUI (same blocker as Phases 3 and earlier in Phase 4 — this dev build isn't a registered app `computer-use` can target).

**Verified:** `bunx tsc --noEmit` clean (re-run independently, not just taken on the agent's word), HMR log shows zero runtime errors across every file it touched, `/status` sidecar still healthy post-change. **Not yet verified:** an actual click-through (Add Soccer Scorebug → confirm it renders correctly → edit scoreboard values in Data Sources → Play In/Out) — same GUI-automation blocker as Phase 3 (unregistered dev build, `computer-use` can't target it by name).

**Verification status (2026-07-11):** TypeScript build clean (npm run build ✓), all 8 sports files present and integrated (soccer, basketball, football, baseball, hockey, tennis, volleyball, rugby + common, lowerThirds, fullscreens, squads). Code-review confirms: all scorebugs are data-driven through binding engine, Brand Kit theming implemented with per-sport color override capability, ticker with seamless-marquee scrolling built. GUI click-through (Add Scorebug → render → edit values → Play In/Out) recommended for final verification on Windows.

## Phase 5 — Virtual Set — COMPLETE (2026-07-11)

Port `legacy/src/components/studio/VirtualStudioScene.tsx`'s 6 set geometries and 3-point lighting rig into document-driven, gizmo-editable scenes (as `set3d` layers, already reserved in Phase 1). Real `@react-three/postprocessing` (the old `DepthPanel.tsx` was 100% cosmetic — new build, not a port). This is the standalone 3D broadcast environment engine: no Unreal/Unity/Godot dependency, targeted at small machines (quality knobs down to 0.5× DPR, shadows/bloom off by default) with broadcast-grade output.

**Acceptance test (written first):**
- **Document model:** `set3d` LayerProps carries a real node graph — `SetNode = model | primitive | text3d | light | camera | videofeed | group` — each with a full 3D transform (position/rotation/scale), plus per-layer environment (background, floor, grid, ambient, fog) and render settings (exposure, shadows, DPR, bloom, vignette). All of it round-trips through the Zod schema without a field being stripped (the known Phase 4 bug class), verified by a script that parses every set builder's output through `projectSchema`.
- **Import (open 3D interchange, what Unreal/Unity/Blender export):** an operator can import `.glb`/`.gltf`/`.fbx`/`.obj` files; the binary is stored by the axum sidecar on disk (never base64 in the SQLite doc) and served at `/assets/{file}` so the editor, Program window, and future OBS consumers all load the same URL. A `model` asset records format + a **real rendered thumbnail** (offscreen WebGL render of the actual geometry, not an icon).
- **Build from scratch:** primitives (box/sphere/cylinder/plane), 3D text, spot/point/directional lights, and virtual cameras can each be added as nodes, selected by clicking in the viewport, and manipulated with a real transform gizmo (translate/rotate/scale modes). One completed gizmo drag = exactly one undo entry.
- **Virtual lighting:** lights are first-class gizmo-editable nodes with color/intensity/angle/penumbra/shadow controls; the legacy 3-point rig (key/fill/rim) ports as three real light nodes, not baked-in constants.
- **Virtual cameras:** multiple named camera nodes per set; one is the *active* program camera; the editor shows frustum helpers and flies free with orbit controls while Program/Preview render strictly through the active camera.
- **Video/AR feeds as environment surfaces:** a `videofeed` node is a plane in the 3D space textured by a **live** source — a capture device (webcam/capture card via getUserMedia), a screen/window share, or a URL/file — usable as LED walls, desk monitors, AR backplates. Feeds are gizmo-positionable like any node and multiple planes can share one stream.
- **No fake templates:** the 6 legacy sets become builder functions emitting fully editable node trees — every desk leg, monitor, and light selectable, movable, deletable. Legacy's fake glowing "CAM 1" boxes become real videofeed nodes.
- **Two-consumer proof:** the same pure `SetNodes` mapping renders in the interactive editor and in the non-interactive `Set3dRenderer` used by Program/Preview through `DocumentRenderer`.
- **Scope note:** the Rust `/program` HTML route continues to render gfx2d only (3D in OBS arrives with Phase 7/8's real video pipeline — consistent with the Phase 3 precedent); the in-app Program window does render the 3D set.

**Built (2026-07-05, this session):**
- **Document model:** `Set3dProps` is now a real node graph — `src/document/types.ts` gained `Vec3`/`Transform3D` (rotation in degrees, converted to radians only at the three.js boundary, matching the 2D degree convention), `MaterialProps`, `VideoSource`, and the seven-node `SetNode` union; `Asset` gained `kind:"model"` + `format` + `thumbnail`. Fully mirrored in `schema.ts` **with `.default()`s on the set3d fields so a pre-Phase-5 bare `{kind:"set3d"}` parses into a valid empty set instead of failing validation and falling back to a blank default project** (no schema-version bump needed — the change is purely additive). All node construction goes through `factory.ts` factories, same discipline as elements.
- **Editor/renderer split, third instance of the pattern:** `src/components/set3d/SetNodes.tsx` is the pure `SetNode → R3F` mapping (no store access) consumed by both `Set3dEditor.tsx` (OrbitControls, click-to-pick, drei `TransformControls` gizmo with translate/rotate/scale modes on any node — models, primitives, lights, cameras, video feeds) and `Set3dRenderer.tsx` (non-interactive, renders strictly through the active virtual camera via a nested drei `PerspectiveCamera makeDefault`, so cameras inside groups get correct world placement for free). `DocumentRenderer` stacks set3d layers as WebGL canvases *under* the Konva gfx2d stage (set = backplate, graphics = overlay — documented v1 simplification).
- **Undo granularity without bookkeeping:** the gizmo mutates the three.js object directly during a drag and the store is written exactly once on mouse-up — one drag = one undo entry with no `pause()/resume()` needed (unlike Konva, whose Transformer writes through React state each frame).
- **Video/AR feeds:** `videoFeeds.ts` refcounts one `MediaStream`+`<video>` per distinct source (capture device via getUserMedia, screen/window share via getDisplayMedia, URL/file) shared across all planes showing it; `VideoFeedView` renders a `VideoTexture` with `toneMapped={false}` (feeds must not be re-graded by the scene's ACES pass) and an honest dark standby panel with the feed label — and the real error string if the source failed — never fake content.
- **Model import (open interchange, what Unreal/Unity/Blender export):** `.glb/.gltf/.fbx/.obj` via three's own loaders. Binaries upload straight from the webview `fetch` to a new axum `POST /assets` (raising axum's 2MB default body limit to 512MB; deliberately not Tauri IPC, which JSON-serializes binaries) onto disk under app-data`/assets/`, served back at `GET /assets/{file}` with CORS + correct MIME for every consumer. Thumbnails are REAL: an offscreen WebGL render of the actual imported geometry at import time (`assetImport.ts`), stored as a small PNG data URL on the Asset; a failed thumbnail imports without preview, honestly. Missing/broken models render the industry magenta wireframe, not silence.
- **Text without the network:** 3D text renders via canvas-texture (`makeTextTexture`), deliberately avoiding drei/troika `Text`, whose default font fetch from a CDN is a real failure mode on this network (see Phase 0 notes).
- **Six legacy sets ported as builders emitting fully editable node trees** (`src/sets/studioSets.ts`) — every desk leg, wall, monitor and light is a selectable, gizmo-movable, deletable node; the legacy fake glowing "CAM 1" boxes are now real `videofeed` nodes on standby until an operator assigns a source. The 3-point rig ports as three real light nodes with rotations computed by `lookAt` math (+Z-toward-subject convention, matched exactly by SetNodes' light-target placement). Every set ships a program camera wired as `activeCameraId`.
- **Panels:** `VirtualSetPanel` (viewport + gizmo mode switch + add-node palette + model import + program-camera selector), `AssetBrowserPanel` (import, real thumbnails, add-to-set), LayersPanel grew an Add-3D-Set builder dropdown + a recursive node outliner, InspectorPanel routes to `SetInspector.tsx` (full node property editing incl. video source picker with live device enumeration; set-level environment/render settings when nothing is selected). Dockview layout key bumped to v5 (virtual-set was previously a placeholder id).
- **Small-machine contract:** per-set render settings — DPR clamp 0.5–2, shadows/bloom/vignette all off by default, `EffectComposer` not even mounted unless an effect is enabled.

**Verified (real function calls, not just compiles):** `bunx tsc --noEmit` clean; `cargo check` clean (only the known pre-existing ndi.rs dead-code warning); full `vite build` succeeds; `scripts/verify-phase5.ts` runs the SHIPPED modules and passed 40/40 checks — all 6 builders structurally sound (rig counts, camera wiring, unique ids), byte-identical Zod round-trip of a project containing every set + a model asset (no field stripping), bare-`set3d` back-compat parse with defaults filled, **light-aim math actually verified** (each rig light's stored Euler really points local +Z at the subject within dot>0.999), and recursive store CRUD on the real `useDocStore` (group-nested update/delete, camera deletion clearing `activeCameraId`).

**Not yet verified:** an eyes-on GUI click-through (import a real GLB, drag gizmos, assign a webcam feed) — same blocker as Phases 3/4 (unregistered dev build, `computer-use` can't target it). The `/assets` POST/GET endpoints were exercised over real HTTP against the rebuilt sidecar (see below). Recommend an operator pass: Add 3D Set → News Desk, click a desk leg, drag all three gizmo modes, undo once per drag, import a GLB, assign a webcam to a monitor feed, switch program camera, confirm Program window renders through it.

**Two real bugs caught by the operator's first eyes-on pass (fixed same session):**
1. **Blank Control Room on cold boot — a latent Phase 3/4 bug, not a Phase 5 one.** `GfxEditor` and `InspectorPanel` called `useDataStore(buildDataValues)` where `buildDataValues` returns a fresh object per call; zustand v5's `useSyncExternalStore` demands a cached snapshot, so a truly fresh mount infinite-looped ("getSnapshot should be cached" → "Maximum update depth exceeded" → crash). It never fired in Phases 3/4 because no session ever cold-remounted those panels; Phase 5's dockview layout-key bump forced the first one. Fix: `useDataStore(useShallow(buildDataValues))` at both call sites, with a comment marking it load-bearing. Lesson recorded: **"HMR clean" is not "cold boot clean" — new panels/layout changes must be verified from a fresh launch.**
2. **Silent sidecar death on dev-watcher restart.** `spawn_output_server` panicked if port 4977 was still held by the previous instance mid-restart — the panic killed only the spawned tokio task, leaving the app running with a working UI and a dead sidecar (no program output, no status, no assets). Now binds with retry (500ms × 60) before giving up; confirmed live by the very next watcher restart succeeding.

**UI overhaul after operator feedback (same session) — the editor now behaves like a DCC tool, not a demo:**
- **Inspector unblocked:** the settings panels were missing `overflow-y-auto` — anything below the fold was unreachable in a docked panel. Rebuilt as a scrollable stack of collapsible categorized sections (Transform / Material / Light / Camera / Surface / Source / Environment / Floor / Render Quality / Effects), Unreal-Details style, with axis-labeled X/Y/Z fields.
- **Dropdowns killed:** the "+ Add node…" select is now an icon place-actors palette (10 node kinds, tooltips); the empty Virtual Set state is a card grid of the six studio sets + Empty Set; LayersPanel's set dropdown is a button grid; outliner rows carry per-kind icons (model/primitive/text/light/camera/feed/group). The program-camera picker stays a compact select deliberately — it's a mode switch, not an asset list.
- **Pro viewport:** drei GizmoViewport orientation axes in the corner (offline-safe — canvas-texture labels, no CDN fetch), live selection bounding box (`THREE.BoxHelper`, tracks gizmo drags per frame), DCC keyboard tools — **W/E/R** move/rotate/scale, **F** frame selected, **Del** delete (respects locks), **Esc** deselect — all suppressed while typing in fields; a status bar showing the selected node and the shortcut legend.
- **Video into the studio, all four signal paths:** Asset kind `"video"` (types + schema + sidecar MIME for mov/m4v); clips import through the same Import button as models (multi-select), get a REAL frame-grab thumbnail, and "Add to Set" drops a feed surface already playing the clip. The feed Source section is tabbed rows, not a dropdown: **Live** (enumerated capture devices with rescan), **Screen** (window/display share), **Clip** (imported videos, with an inline import that assigns to the surface immediately), **URL**, **Off**.

**Verified after the overhaul:** `tsc --noEmit` clean, watcher `cargo` rebuild clean, `scripts/verify-phase5.ts` still passes fully against the extended schema, HMR applied every panel with zero runtime errors, sidecar healthy after the bind-retry restart. GUI click-through of the new toolbar/inspector still pending operator eyes.

**Third real bug caught, this one live-verified end-to-end (2026-07-05): camera/mic never worked in ANY Tauri window, on any phase.** `getUserMedia()` doesn't fail fast with a clean error in this app's WebView2 host — it hangs forever, neither resolving nor rejecting, because WebView2 has no default permission UI in a chromeless embedded host and nothing was registered to answer the request. This blocked every `videofeed` node's "Live" camera tab silently (an operator would just see it never connect, no error to act on). Confirmed via WebView2's CDP remote-debugging port (`WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9222`, connect on `127.0.0.1` not `localhost` — the latter resolves IPv6 first and the debug server only binds v4) evaluating `getUserMedia` directly in the live running Control Room window — a real, reusable diagnostic technique for this app going forward, since `computer-use`-style GUI automation can't target this unregistered dev build by app name.

**Fix:** `src-tauri/src/lib.rs`'s new `grant_media_permissions()` (Windows-only, `#[cfg(windows)]`) registers an `ICoreWebView2::add_PermissionRequested` handler on every window in `.setup()`, auto-approving `Camera`/`Microphone` kinds only — correct for this app specifically because every window here is our own trusted first-party content, never third-party. New pinned deps: `webview2-com = "=0.38.2"`, `windows = "=0.61.3"` (must match tauri/wry's own resolved versions exactly, since `with_webview`'s `ICoreWebView2Controller` type has to be the identical type at the call site). **Verified live, not just compiled:** after the fix, `getUserMedia({video:true})` resolved with the real device (`"HP HD Camera (0408:5373)"`) instead of hanging, and `enumerateDevices()` correctly listed all three real capture sources on the machine including **OBS Virtual Camera** — confirming "other camera sources" (virtual cams, additional USB webcams) need no extra code, they're already covered by the existing `device` source type once permission actually resolves.

**Fourth real bug, found by the operator and diagnosed by CDP inspection, not guessing: multiple simultaneously-visible `set3d` layers silently stack and hide each other.** `DocumentRenderer.tsx` renders every `visible: true` `set3d` layer in a scene as its own full-screen absolutely-positioned WebGL canvas, stacked in zIndex order. With no mutual-exclusivity rule, an operator experimenting with "Add 3D Set" (News Desk → Election HQ → Sports Arena, all in Scene 1) ends up with 3 opaque canvases stacked on top of each other — only the topmost (**highest zIndex**, Sports Arena) is ever visible in Program/Preview, completely hiding News Desk underneath even though News Desk's model + webcam were pushed correctly and rendering live in the editor. Confirmed with certainty by fetching the live `/document` envelope and dumping each layer's `zIndex`/`visible`/`environment.background` — not inferred, read directly off the wire. This produced two distinct operator-visible symptoms depending on framing ("assets/video stuck in old" and, separately, "3D studio preview is not live") that were actually the same root cause: whichever layer is topmost was static (an unconnected standby videofeed + a default text3d node), so it visually reads as "frozen," while the genuinely live layer underneath is fully invisible.

**Fix:** new `addSet3dLayer(sceneId, layer)` store action (`store.ts`) hides every other `set3d` layer in the scene when a new one is added; `addLayer(sceneId, "set3d")` (the "Empty Set" button) gained the same guard inline. Both `LayersPanel.tsx` call sites for adding 3D sets ("3D Sets" template grid and "Empty Set") now route through this. A scene shows exactly one virtual set at a time going forward, matching how a real broadcast scene works. **Verified live for the operator's actual already-stacked session** (not just for future additions): used the CDP link to locate and click the real visibility-toggle buttons for Election HQ and Sports Arena in the live Control Room DOM, re-fetched `/document` and confirmed both flipped to `visible: false` while News Desk (with the real model, webcam, and two video-clip feeds) stayed the only visible set3d layer.

**Related, ruled out rather than assumed:** the operator also reported seeing `THREE.WebGLRenderer: Context Lost.` in the console and worried it was a live/recurring failure. Investigated by polling `canvas.getContext('webgl').isContextLost()` directly across multiple checks — confirmed `false` (healthy) every time; the log lines were the same 1-2 historical entries replaying on every fresh CDP `Runtime.enable` (a known Chrome DevTools Protocol behavior: buffered console messages replay to a newly-attached debugger), not new events. Reducing to one visible `set3d` layer per scene also directly reduces simultaneous WebGL context count (each hidden layer now fully unmounts via `DocumentRenderer`'s `.filter(l => l.visible ...)`, properly disposing its context rather than idling).

**Fifth, minor, real bug — the recurring "key prop spread into JSX" React warning the operator kept seeing:** `renderNodes.tsx`'s `common` props object included `key: el.id` and was spread into `<Rect {...common}>`/`<Text {...common}>` — React requires `key` be passed directly at the JSX call site, never through a spread. Fixed: `key` removed from the shared `common` object, applied explicitly (`key={el.id}`) at every JSX return site (`rect`, `text`, `image`, `group`). Cosmetic (didn't affect correctness) but real and now silenced — confirmed via a fresh CDP console listen showing zero key-spread warnings post-fix.

**Confidence monitor feature added (2026-07-05), by request — a real screen/monitor node inside a virtual set can now show a live re-render of Program or Preview, matching real vMix/OBS multiviews.** `VideoSource` gained `{ type: "program" }` / `{ type: "preview" }` (mirrored in `schema.ts`). `ConfidenceMonitorView` (new, `SetNodes.tsx`) renders the target on-air scene's visible `set3d` layer into a drei `RenderTexture` applied to the monitor's plane — a genuine re-render through the target's own active camera (reuses `SetNodesView`/`CameraNodeView`'s existing camera-selection logic unchanged, so it's not a special case), not a captured pixel stream. `SetNodeContext` grew `project`/`programSceneId`/`previewSceneId`/`confidenceDepth`, threaded through both consumers (`Set3dEditor.tsx`, `Set3dRenderer.tsx` via `DocumentRenderer.tsx` via `ProgramView.tsx`/`PreviewView.tsx`'s existing envelope data — no new data source, just wiring already-available fields further down). `SetInspector.tsx`'s Source picker gained **PGM**/**PVW** tabs alongside Live/Screen/Clip/URL/Off — assignable to any existing monitor node (CAM 1, DATA 1, etc.), not a new dedicated node type, so an operator can turn any existing screen into a confidence monitor.

**Recursion is real and deliberately bounded, not avoided.** A monitor confidence-viewing the very scene it physically sits inside genuinely recurses (a camera pointed at its own screen would tunnel forever in real life too) — capped at depth 1 via `confidenceDepth`: the nested copy renders a standby ("nested confidence view (depth limit)") instead of recursing further, a stated v1 bound rather than a silent limitation.

**Verified live end-to-end, including a real UI click-through** (not just typecheck): cold-booted the app, used the CDP link to click through the actual running UI — selected a `News Desk` videofeed node in the outliner, confirmed the Inspector's Source section showed the new PGM/PVW tabs, clicked PGM, and confirmed via the live `/document` envelope that the node's source became `{"type":"program"}`. This is the self-recursive case (News Desk is both the on-air scene and the monitor's own location) — confirmed zero console errors in both Control Room and Program windows afterward, and confirmed the real WebGL canvas in each stayed healthy (`isContextLost() === false`), meaning the depth-1 cap is holding and not runaway-recursing.

**Sixth "bug" report, actually correct on-air discipline working as designed — diagnosed, not just asserted.** Operator report: "the LIVE badge and scoreboard show in the GFX Editor but not in Preview/Output." Confirmed via the live `/document` envelope: `Soccer Scorebug` (which owns the LIVE badge, HOME/AWAY score, and clock — verified by dumping its actual `elements` array) has a `timeline`, and `layerPlayback` was `{}` — it had never been "Played In". Per this project's own Phase 3 design ("a timelined layer is off-air until it's actually been triggered" — `DocumentRenderer.tsx`), the GFX Editor deliberately shows every layer at rest for authoring regardless of on-air state, while Preview/Program correctly gate a timelined layer behind an explicit Play In/Out command — exactly like a real playout system. Not a bug. Clicked "Play In" for the operator via the CDP link and confirmed `layerPlayback` populated (`{"phase":"in","startedAt":...}`) — the graphic is now on-air.

**Video-in-2D-graphics feature added (2026-07-05), by explicit request — a live feed can now be a first-class element inside the flat GFX canvas itself, not just a monitor prop inside a 3D virtual set.** New `VideoElement` (`kind: "video"`, carries the same `VideoSource` the 3D `videofeed` node uses — device/screen/url/program/preview) added to the gfx2d `Element` union, mirrored in `schema.ts`, with `createVideoElement()` in `factory.ts`. `renderNodes.tsx` gained `VideoElementView` — the one hook-using leaf in an otherwise-pure element mapping (same pattern as SetNodes.tsx's `VideoFeedView`): a Konva `<Image>` fed by `useVideoFeed`'s video element, with its own `requestAnimationFrame` loop forcing `batchDraw()` every frame, since Konva has no way to know the underlying `<video>` advanced a frame on its own. `program`/`preview` sources are accepted for source-picker consistency but render an honest "not supported in 2D graphics yet" standby — a real 2D confidence monitor would need to recursively re-render the whole Konva stage, deliberately deferred rather than faked.

**Real refactor along the way:** the video source picker UI (Live/Screen/Clip/URL/PGM/PVW/Off tabs, device enumeration, clip import) was duplicated logic waiting to happen — extracted from `SetInspector.tsx` into a new shared `VideoSourceEditor.tsx`, now used by both the 3D `videofeed` node inspector and the new 2D `video` element inspector (`InspectorPanel.tsx`). `LayersPanel.tsx` gained a "+ Video" button alongside "+ Rect"/"+ Text".

**Verified live end-to-end, including a real UI click-through, not just typecheck:** cold-booted the app, used the CDP link to click "New Layer" active, click "+ Video", confirmed the Inspector showed the new element with the full Source picker (Live/Screen/Clip/URL/PGM/PVW/Off), clicked the "Clip" tab, clicked the existing REAL MADRID vs RC CELTA clip asset, and confirmed via the live `/document` envelope that the element's `source` became the real asset URL. Zero console errors in both Control Room and Program afterward; both windows' real WebGL canvases stayed healthy (`isContextLost() === false`).

## Phase 5.5 — Control Surface & Compositing Pass — BUILT (2026-07-05, operator-approved scope)

Operator-driven fix/feature pass after the first Phase 5 eyes-on session. Scope refined and approved before build (see the seven numbered items in the session log).

1. **Layers panel redesign:** kind icons everywhere, double-click inline rename (layers, elements, 3D nodes), and the active layer gets a labeled action bar — Up/Down, Play IN/OUT, **Duplicate** (new `duplicateLayer` store action deep-cloning with fresh ids and remapping `activeCameraId` onto the cloned camera), **Delete** in red with a real hit target. The old delete existed but was one of seven 12px icons — a discoverability failure, not a missing feature.
2. **Images end-to-end:** `.png/.jpg/.webp` import through the sidecar (`webp` MIME added), real downscaled-bitmap thumbnails, "+ Image" opens a thumbnail-card picker dialog (no dropdown), `renderNodes` draws the actual bitmap via a shared `imageCache.ts` (one HTMLImageElement per URL). Editor shows loading/missing/broken placeholders; on air those render nothing.
3. **Templates panel** (own dockview panel, cards + sport chips, zero dropdowns): four **animated full-screen** graphics — Matchup, Starting Lineup, Stat Board, Final Score (`src/sports/fullscreens.ts`) — plus the 8 scorebugs and ticker relocated from LayersPanel as cards. Every dynamic field is a real Binding (new shared `FULLSCREEN_DEFAULTS` keys — matchTitle/venue/player1-6/stat1-4 — merged into all 8 sport sources, live-editable in Data Sources), panels bind to the Brand Kit, IN/OUT via the existing timeline engine, inserted layers fully editable. `RectElement.gradient` (mirrored in schema) adds linear-gradient backdrops.
4. **GFX canvas composites over the live set:** GfxEditor renders the scene's set3d layers through their program cameras beneath the Konva stage — graphics are authored against the actual program picture. Toggleable ("SET" button, on by default).
5. **Clean on-air output:** standby/no-signal panels (2D video elements, 3D feed surfaces, confidence-monitor fallbacks) are editor-only; on Program/Preview an unassigned/failed source renders nothing (3D keeps the physical bezel + plain dark screen). This kills the "video layer blocking the set" the operator hit.
6. **Program = Preview framing:** ProgramView now fit-scales exactly like PreviewView — it used to render 1:1 and crop when its window was smaller than project resolution, which read as a "different camera angle." Same scene + same camera now provably render the same frame.
7. **Cameras panel:** every camera node across the scene's sets — rename inline, scrub-friendly Pan/Tilt/Roll/X/Height/Z/FOV sliders+inputs, one-click **CUT** with red tally on the live camera, add/delete cameras. Backed by the same nodes the viewport gizmo edits. Dockview layout key bumped to v6 (templates + cameras panels).

**One real bug caught by verification again (the recurring factory class):** `createRectElement` didn't copy `overrides.gradient` — every template backdrop silently lost its gradient while compiling clean, exactly like Phase 4's `scrollSpeed`. Caught by the extended verify script, which also exposed that Zod re-orders keys on parse (schema order vs authored order) — the round-trip check now compares canonicalized values (`stableStringify`), which is the honest form of "nothing stripped."

**One more real environment bug, caught and fixed during the cold-boot check:** the Tauri webview raced Vite's lazy dependency re-optimization (drei's deep imports triggered a mid-session re-bundle) and got "504 Outdated Optimize Dep" on its module URLs — Control Room never mounted, no document push. Fix: `optimizeDeps.include` in vite.config.ts pre-bundles every heavy dep (three/fiber/drei/postprocessing/example loaders/konva/zustand/gsap/dockview) so the optimizer never re-runs mid-session; verified by a subsequent clean cold boot that mounted and pushed the persisted document.

**Verified:** `tsc --noEmit` clean; `cargo check` clean; `scripts/verify-phase5.ts` extended (sections 6–7) and fully passing — all 32 sport×template combinations have every bound key resolving against the REAL flattened data sources (a typo'd key can't ship), elements in frame, timelines present; templates+image+video project round-trips the schema value-identical; gradient survives factory+schema; duplicateLayer produces fresh ids and remaps the program camera. Cold-boot relaunch performed (not just HMR — the Phase 5 lesson). **Pending operator eyes:** the new panels' look/feel, gizmo-vs-Cameras-panel interplay, and an actual webcam/clip assignment.

**Post-Phase-5 operator session (2026-07-05): confidence monitors, 2D video elements, and real environment/UX bugs found by actually using the app.**

- **Confidence monitors (Program/Preview feeds on a 3D monitor screen):** `VideoSource` gained `{type:"program"}`/`{type:"preview"}` (`types.ts`, mirrored in `schema.ts`). `ConfidenceMonitorView` (`SetNodes.tsx`) renders a `drei` `RenderTexture` re-rendering whichever scene is on Program/Preview onto the monitor's plane — genuinely recursive when the monitor is inside the very scene it's monitoring, so `SetNodeContext.confidenceDepth` caps it at 1 level (the nested copy shows a standby, not an infinite tunnel — a stated bound, not a silent one). Wired through `Set3dRenderer`/`DocumentRenderer`/`ProgramView`/`PreviewView` so `project`/`programSceneId`/`previewSceneId` reach the monitor. New "PGM"/"PVW" tabs in `SetInspector.tsx`'s source picker. Verified live: assigned PGM to a real monitor node, confirmed via the pushed document and a clean console (no crash from the self-referential render).
- **2D video elements (a live feed as a GFX-canvas element, not just a 3D monitor prop):** new `VideoElement` (`kind: "video"`, same `VideoSource` shape) in the gfx2d `Element` union. Real bug caught mid-build and fixed: axum's `asset_get_handler` (`lib.rs`) read whole files into memory with no `Range` support — WebView2's media pipeline refuses to play a `<video>` source at all without `206 Partial Content` responses, failing with "no supported source was found" instead of a clear error. Added `parse_byte_range` + real seek-and-read Range serving (verified live: `curl -H "Range: bytes=0-1023"` returns a genuine `206`/`Content-Range`, and the same clip that failed before now loads with no console error).
- **New `reorderElement` action** (`store.ts`) — elements had zIndex-free array-order stacking with no way to reorder them (no send-to-back/bring-to-front), a real gap once a background video plate needs to sit behind existing graphics. `LayersPanel.tsx` element rows gained "Send backward"/"Bring forward" buttons mirroring the existing layer-reorder pattern.
- **Corrected scope mid-build:** the operator's actual ask was the 2D graphics compositing over the *already-visible* 3D Virtual Set (which `DocumentRenderer`'s existing set3d-backplate + gfx2d-overlay stacking already does for free) — not a second, separate flat video duplicating the same clip on the 2D canvas. Added and then removed that element once the actual intent was clear; logged here so the distinction isn't lost.
- **Two real environment gotchas hit repeatedly this session, worth recording:** (1) `layerPlayback` (on-air Play In/Out state) is deliberately not persisted to SQLite (see `playbackState.ts`'s own comment) — every dev-server restart resets it, which repeatedly looked like "graphics stuck on old" when it was really "never played back in after the last restart." (2) A page reload mid-session (WebView2/Vite HMR) can desync the Tauri IPC callback bridge (`[TAURI] Couldn't find callback id ...` in console) — clicks still update local React state but `invoke()` pushes silently fail, so Program/Preview stop reflecting *any* change until a full process restart. Neither is a app-logic bug; both cost real debugging time before being identified, so recording them here for the next session.
- **UX fix, not a bug:** Play In/Out were only rendered inside the *active* layer's expanded action bar (`isActiveLayer &&` in `LayersPanel.tsx`) — invisible for every other layer, including ones already on-air. Moved to the always-visible top row (same as Eye/Lock), gated only on `layer.timeline`. Verified live: both Soccer Scorebug's and Ticker's IN/OUT buttons now show simultaneously while a third, unrelated layer is active.

## Phase 5.6 — Broadcast-Grade Motion Graphics Pass — BUILT (2026-07-05, operator-approved scope)

Operator verdict on the 5.5 templates: "not animated, raw, generic." Root causes were engine-level, fixed at the engine level. Reference image (layered skewed-bar broadcast lower thirds) downloaded and studied directly; the "download ready-made templates" route was evaluated and rejected — stock AE/vector packs can't bind to live data or the Brand Kit, so the visual language is replicated natively instead.

1. **Per-element animation engine** (`ElementAnim` on every 2D element: in/out with delay, duration, direction, distance, GSAP ease, fade) resolved inside the same pure `applyPlayback` — bars wipe in, kicker tabs drop in late, text slides staggered, rows cascade (~1.1–1.4s IN, faster OUT, per broadcast convention). Elements without a spec keep the legacy layer-wide slide/fade; groups recurse. Semantics: before its delay (IN) / after its exit (OUT) an element is simply off screen, not visibly parked at its offset.
2. **Visual toolkit:** `skewX` on rects (degrees, converted to Konva's tangent at render — the reference's parallelogram bars), 3-stop gloss gradients (`gradient.mid`), drop shadows on rect/text/image, `letterSpacing` + render-time `uppercase` on text. All schema-mirrored, factory-copied (the twice-bitten field-drop class), and operator-editable via new Inspector sections (Animation / Shadow / Shape FX / Typography FX in `ElementFxInspector.tsx`).
3. **Lower-thirds pack** (`src/sports/lowerThirds.ts`), modeled on the reference rows: Breaking News (channel tab + navy gloss headline bar + top-edge kicker + offset sub-strip), Standard two-tier, LIVE tag (inverted palette), Sports Score (graphite + team-color panels + gold accents, bound to the selected sport's live score/clock). Realistic per-design palettes — deliberately NOT all chained to one Brand Kit color (operator direction); new mock-feed keys kicker/subline/channel/tagline, live-editable.
4. **Full-screens restyled** in the same language: skewed brand panels, gloss accents, shadows, staggered row cascades (80–90ms steps), letter-spaced uppercase titles with back-eased drops.
5. **Templates panel previews are now REAL:** each card renders the actual template's elements resolved against live data through the shared renderNodes pipeline at thumbnail scale — the hand-drawn div mockups are gone; inserting re-runs the builder for fresh ids.

**Verified:** `tsc` clean; `scripts/verify-phase5.ts` extended to 8 sports × 8 templates (4 full-screens + 4 lower thirds — every bound key resolves, value-identical schema round-trip covering anim/shadow/skew/gradient.mid/letterSpacing/uppercase) plus a **section 8 choreography suite running the real `applyPlayback`**: all elements hidden at t=0, main bar mid-flight left of authored while the delayed kicker is still off at t=0.25, everything exactly at authored state when settled, staggered OUT (text fades before the bar moves), all gone at OUT end, and the legacy anim-less fallback intact. Cold-boot relaunch performed. **Pending operator eyes:** the actual motion feel on Play In/Out and the new template looks.

## Phase 5.7 — Show-graphics breadth + from-scratch authoring — BUILT (2026-07-06, operator-approved scope)

Operator's list, worked top to bottom. All frontend/TypeScript (no Rust touched); `tsc --noEmit` clean, `scripts/verify-phase5.ts` extended and fully passing, and every item verified live via CDP against the running app (zero console errors, all 20 canvases context-healthy afterward).

1. **Genre template families beyond sports** (`src/genres/`) — News/Politics (Election Strap L3 + Results Board full-screen), Weather (Conditions Strap + 5-Day Forecast Board), Program (Up Next Strap + Presenter name-strap + Coming Up Rundown), Faith/Events (Scripture Board + Speaker Strap + Worship now-playing). Each binds to its own live feed source (`politics`/`weather`/`program`/`event`, already scaffolded in `dataSources.ts`) with realistic per-genre palettes (navy/red news, teal/warm weather, purple/magenta promo, plum/gold faith) — deliberately NOT chained to the Brand Kit (that's the sports-chrome convention). The skewed-gloss geometry + choreography was extracted from the sports lower-thirds into a shared `src/graphics/motionKit.ts` (L3 language) + `src/genres/genreKit.ts` (full-screen boards), and `lowerThirds.ts` refactored to consume it (verify suite confirmed no regression). Templates panel gained top-level category tabs (Sports/News/Weather/Program/Faith); genre cards render the same live-data previews as sports. **Verified live:** all 5 category tabs present, genre previews render (real Konva canvases, no errors), Election Strap inserts (12 elements) into the scene and reaches the `/document` output envelope.

2. **Image slots + Inspector "Choose image"** — `createImageSlot()` factory makes an unfilled placeholder (empty `assetId` → dashed "empty slot" box in editor, nothing on air). InspectorPanel now renders an `ImageSlotEditor` for image elements: current-fill thumbnail (or placeholder) + a "Choose image" button opening the shared `ImagePickerDialog` (two clicks to fill a slot). **Verified live:** selecting an image element shows `(image)` + the "Choose image" button + "empty slot" label. **Also fixed a pre-existing breakage found here:** `LayersPanel.tsx` still had a leftover local `ImagePickerDialog` duplicate (from the earlier extraction to its own file) referencing removed imports — `tsc` was failing on it; deleted the dead copy.

3. **Modern unified scorebug** (`src/sports/scorebug.ts`) — one `createScorebug(sport, label)` builder replaces the 8 flat Phase-4 per-sport bands: skewed gloss panels (skewX −12), drop shadows, graphite score block with a gold sliver, team-color accents, choreographed IN/OUT (score block drops center-first, panels wipe from the sides, labels stagger, clock tab rides in last). Bound to each sport's 6 shared core keys; panels still bind `brand.panelBg` so the Brand Kit re-themes them. Each `src/sports/<sport>.ts` shrank to just its KEYS + DEFAULTS + a one-line delegating `create<Sport>Scorebug()` (extras like `fouls`/`down`/`balls` stay in DEFAULTS for the stat/full-screen templates). **Verified live:** a freshly-inserted Soccer Scorebug = 14 elements, all 7 rects skewed −12, bound to `brand.panelBg` + all 6 `soccer.*` keys.

4. **Photoshop-style authoring in the GFX editor** — floating element toolbar (rect/text/image-slot/video, duplicate, bring-forward/send-backward, and 6 align-to-canvas buttons), window-level keyboard handlers (arrow-nudge 1px / Shift 10px, Ctrl+D duplicate, Del/Backspace delete — all yielding to focused inputs), and double-click-to-edit text inline on canvas (a positioned textarea over the element's exact rect, commit on blur/Enter, Esc cancels). New `duplicateElement` store action + exported `duplicateElementValue` factory (deep clone, fresh ids, nudged). Build graphics from scratch without templates. **Verified live:** the 13-button toolbar renders; inserting a rectangle lands it at canvas-center (y=470) and Align-left sets x=0.

5. **Smart-crop thumbnails** — was already implemented (`contentBounds` in `TemplatesPanel`); confirmed still correct (lower thirds fill their cards).

**Verify-suite extensions:** `scripts/verify-phase5.ts` now also checks all 4 genre categories' templates (every bound key resolves against the real flattened data sources, elements in-frame, timelines present) and all 8 unified scorebugs (bound keys resolve, timeline present, skewed gloss panels present). Cold-boot relaunch performed (the recurring Phase 5 lesson — not just HMR). **Pending operator eyes:** the actual motion feel of the genre packs and modern scorebug on Play In/Out, and filling a real logo/photo into an image slot. **Note:** a handful of demo inserts (test rectangle, empty image slots, an Election Strap, a modern scorebug) were left in the running dev project from live verification — undo or delete them as desired.

## Phase 5.8 — Operability & Delegated Expansion — BUILT & VERIFIED (2026-07-06)

**Verified:** `bunx tsc --noEmit` exit 0 across the whole integrated tree (two parallel sessions' work + two subagents + this thread — including a build-breaking dead-function error in a parallel session's `playout.ts` that this thread fixed by wiring `endStatusFor` into `stop()`/`next()` per the author's evident live-item-completion intent). `scripts/verify-phase5.ts` ALL CHECKS PASSED — extended to cover the modern corner scorebug and every genre pack (politics/weather/program/faith) with real binding-key resolution against the live data sources, plus the existing per-element choreography and schema round-trip suites.


Operator-reported issues + first multi-agent pass (two Sonnet subagents with scoped file lists, explicit patterns, offline constraints and fallback routes — per MODEL_STRATEGY.md, flagged in-session).

**Fixed inline (main thread):**
- **"Deletes don't work / templates don't save" diagnosed honestly:** persistence was verified HEALTHY (scripts/check-db-load.ts reads the real studio.db through the real load schema — the operator's project loads clean). The actual gaps were UX: no scene delete/rename existed at all (added: `removeScene` with PGM/PVW/active re-pointing + guard against deleting the last scene, `renameScene`, hover-delete + double-click rename on scene rows), no Delete button in the element Inspector (added, red, next to the element name), and **no save-as-template feature existed** — now real: migration v4 `user_templates` table, `userTemplates.ts` zustand store (rows validated through the exported `layerSchema`, corrupt rows skipped loudly), "Save Template" in the Layers action bar, "My Templates" cards (real previews, insert-with-fresh-ids, delete) at the top of the Templates panel.
- **Flicker:** Program/Preview polled every 1s and rebuilt the whole envelope object → full re-render/flicker even when idle. Both views now dedup on the raw response text — an unchanged document renders zero times.

**Delegated (both agents completed, integrated by main thread):** Agent A (3D realism + AR) delivered all four realism knobs with NO fallbacks taken — `RenderSettingsApplier` sets `PCFSoftShadowMap`; drei `ContactShadows` in `SetEnvironmentView`; `N8AO` (confirmed exported by the installed `@react-three/postprocessing`) in `SetPostEffects`; offline `Environment` + 3 `Lightformer` panels for real PBR reflections — all in `SetRenderSettings` (schema `.optional()` + factory defaults) and editable in a new SetInspector "Realism" section. AR backplate: `SetEnvironment.backplate: VideoSource` painted as `scene.background` (flat `<color>` suppressed while active), new `ARPanel` (source picker + AR/Studio presets), layout key → v8. Agent B delivered `src/genres/politics.ts` + `src/sports/scorebugModern.ts` (compact corner bug, skewed gloss, per-element choreography). **Integration cleanup by main thread:** wired `createModernScorebug` into the Templates panel as a featured "Corner Scorebug" card (the flat Phase-4 bug is now "Classic Scorebug"); found Agent B's `politics.ts` was byte-identical to a pre-existing `news.ts` from a parallel session (same 10 `politics.*` bindings) → deleted the duplicate `news.ts` and dropped the redundant "News" category, keeping "Politics" (the genre the operator actually named). Verify script extended to cover both scorebug variants + all genre packs against the real data sources.

**The lesson recorded (multi-agent coordination):** two Sonnet agents on the same tree is efficient but needs the main thread to (a) hand each a disjoint file list, (b) mid-course-correct when one discovers pre-existing work (done here via SendMessage), and (c) do a dedup/integration pass at the end — parallel agents can't see each other's files, so identical-content collisions (news.ts vs politics.ts) are the main thread's job to reconcile, never the agents'.

## Path to Complete & Competitive (refreshes Phases 6–8)

**The single highest-impact gap:** OBS currently sees the Rust-rendered `/program` HTML snapshot (rect/text only, no animation, no 3D) while the in-app Program window shows the full engine. **Phase 7 priority #1: serve the REAL renderer to OBS** — point `/program` at the actual ProgramView bundle (dev: redirect to the vite URL with a `?window=program` route; packaged: sidecar serves the built assets). One change makes OBS output pixel-identical to the engine incl. 3D, choreography, video feeds. Browser Source IS a professional delivery path (it's how Singular.live/Flowics ship); NDI is additive, not a prerequisite.

**SDKs / external deps (what to download, honestly):**
- **NDI SDK 6** — ✅ NO LONGER A STUB. A parallel session replaced `ndi.rs` with a real 338-line implementation that dynamically loads the installed NDI runtime (falls back to an honest "NDI SDK not installed" status when absent); `/status` on the running build now reports `ndi.available: true`. Remaining: feed it real program frames (currently the sender exists but frame source is the next wire-up — window/offscreen capture first, frame-accurate GPU readback in Phase 8).
- **Spout (Windows GPU texture share)** — open source, vendorable, cheaper than NDI for local OBS; Phase 7.
- **FFmpeg** — bundle a sidecar binary for recording/streaming; Phase 8.
- Everything else already ships via npm/crates — no other SDKs missing.

**Remaining competitive checklist:** scene transitions (crossfade/stinger between PGM scenes), WS push replacing 1s polling (Phase 7 control server + Bitfocus Companion), multiviewer window, project manager (new/open/save-as/export file), audio (explicitly out of scope until after video is complete), 1080p60 hardening + signed installer (Phase 8). **UI completeness pass:** native app menubar (File/Edit/View), keyboard-shortcut cheat sheet, consistent empty states — the "feels incomplete" polish, scheduled with the multiviewer.

## Phase A/B/C — Workspace pages + Timeline + Playout — BUILT (2026-07-06, operator-approved architecture)

Operator verdict: the single ~12-panel Control Room dockview was too cramped for real graphics/AR/timeline work, "everything on one space." Reworked to a workspace-pages architecture (design-time vs show-time split), then built out the two missing show-time surfaces.

**Phase A — workspace shell (the de-cramping):** `ControlRoomView` is now a thin frame — a `PersistentShell` strip (workspace switcher + always-reachable show controls: undo/redo, Take/Cut, REC placeholder, NDI health, real ON-AIR lamp; folds in the old `DocumentStatusBar`) over exactly ONE mounted workspace page. Six pages (`src/document/workspace.ts` store, persisted): **Design** (GFX canvas + Layers/Inspector/Templates/Assets), **Studio** (3D/AR viewport + Cameras/AR/Inspector/Assets), **Data** (data sources full-size), **Timeline**, **Playout**, **Show** (embedded PGM/PVW `MonitorsPanel` + Program Control + Output + NDI). Each page owns its own dockview instance + saved layout (`WorkspaceDockview` + `workspaces.tsx`), keyed so switching fully unmounts the previous page (no cross-page bleed, no double-mounted GfxEditor). The panels themselves are unchanged — only how they're grouped. New `MonitorsPanel` embeds the real `DocumentRenderer` PGM/PVW feeds off the live store.

**Phase B — Timeline** (`TimelinePanel` + `src/document/sequence.ts`): a real track sequencer for the active scene. One track per layer; each layer's clip is `[inTime, outTime]` (draggable/resizable, persisted per scene). The transport walks a playhead across the tracks and fires the ACTUAL `playIn`/`playOut`/`resetPlayback` (new store action) as it crosses each clip edge — so playing/scrubbing here genuinely animates Program & Preview, not a mock. Ruler, scrub-to-click, length control, live-clip highlight.

**Phase C — Playout** (`PlayoutPanel` + `src/document/playout.ts`): rebuilt from a scene-playlist into a real station **rundown** per operator direction ("line up upcoming programs… current program must be recorded so the station knows what's coming, what ended, and can extract logs"). Programs (not scenes) with `type` (program / live / clip / station id / break / filler), an aired `sceneId`, and a planned duration, laid out with **projected time-of-day on-air times** computed from an editable schedule start (`projectedStartSecs`). A **headless transport ticker lives in the module** (not the component) so transmission keeps running when the operator leaves the Playout page; it auto-advances back-to-back (optional loop), maintains **Now/Next** (on-air elapsed/remaining + progress, next title + projected start + starts-in), and writes a full **as-run log** — planned vs **actual** start/end, actual duration, and status (completed / cut / skipped / on-air) — one entry per aired item. **CSV export** (`downloadAsRunCsv`) extracts the log for compliance/billing. Inline-editable rundown (title/type/scene/duration, reorder/duplicate/remove/take-to-air), Rundown + As-Run tabs. Persists items + log + schedule start; transport comes up stopped on reload (safe default). The Show page still overrides live via `setProgramDirect`.

**Verified live via CDP (cold reload, then click-through):** all 6 workspace tabs render; the shell shows Take/Cut/REC/NDI/ON-AIR; Timeline mounts 9 track clips for the active scene, Play advances the playhead (`0:00`→`0:05.6`) and clips transition live→out at their out-points; the rebuilt Playout rundown renders Now/Next + schedule start + projected end; adding 2 programs (2s each, scenes assigned) and pressing Play aired program 1, auto-advanced to program 2, and stopped at end of schedule — a clean run produced **exactly 2 as-run entries** (both `completed`), each recording projected time-of-day, actual start/end, actual duration and status (no duplicate ticker after a real page load); Export CSV enabled. Zero console errors, dockview stable. `tsc --noEmit` clean. (One HMR gotcha hit during dev, not a code bug: `initPersistence`'s `initialized` module guard + an HMR store reset can leave `project` null until a full page reload — production cold-boot runs it once and loads fine.) **Pending operator eyes:** the embedded PGM/PVW monitors' live render on the Playout/Show pages, and Timeline clip choreography feel. **Deferred (Phase D):** tear-off windows/multi-monitor, record/export from the timeline, promoting the sequence/playlist stores into the project document.

## Phase 5.9 — Asset System Separation + Font Support + UI Text Cleanup — BUILT (2026-07-06)

Operator direction: the asset system must be separated into its own respective sections (3D Assets / Images / Video / Fonts / Graphics), and self-explanation must come out of the UI.

**Asset system split (`AssetBrowserPanel.tsx` rewritten):** five distinct tabs, never a mixed grid — 3D Assets, Images, Video, Fonts, Graphics. Each tab has its own import affordance (filtered file-picker `accept`) and its own "use it" action (Add to Set / Add to Canvas / none for fonts, which are a pure library). The Images tab keeps the AI-generate box scoped to itself rather than floating above a mixed grid. The Graphics tab is a new home for the existing `useUserTemplates` store (saved layers from "Save Template" in the Layers panel) — insert-with-fresh-ids + delete, reusing the same real-render preview as Templates panel's My Templates (not a duplicate implementation — see next item).

**Fonts made real, not decorative:** `Asset.kind: "font"` existed in the type/schema since Phase 5 but had zero import/render/apply path — an inert declaration. Now: `assetImport.ts` gained `importFontFile` (upload via the existing sidecar, derive a family name from the filename), a new `src/document/fonts.ts` registers every font asset exactly once via the real `FontFace` API (`useRegisterFonts` hook, called independently in `GfxEditor`, `DocumentRenderer`, and the font picker — each top-level window/document registers its own project's fonts), and the Rust `asset_mime` gained `ttf/otf/woff/woff2`. The Fonts tab's cards render `Aa Bb` in the actual registered typeface — the project's "real preview, never an icon" rule applied to fonts. Closing the loop: added a **Font field to the text-element Inspector** with a card-picker dialog (`FontPickerDialog.tsx`, mirrors `ImagePickerDialog.tsx`) listing system faces (Geist Sans/Mono, sans-serif, serif, monospace) plus every imported font, each previewed in its own face — import a font and it's immediately usable on text, no dead-end library.

**Shared preview extracted:** `TemplatesPanel.tsx`'s local `TemplatePreview`/`contentBounds` (smart-crop to content bounds, real Konva render) moved to `GraphicPreview.tsx` so the Templates panel and the Assets panel's Graphics tab render identically from one implementation, not two copies that could drift.

**Self-explanation stripped from the UI** (operator: "remove self explanation from the UI") — removed always-visible narrative/tutorial blocks that explained what a panel or feature IS in prose, keeping only actionable labels, empty-state one-liners, and on-hover tooltips (which are opt-in, not narration): ARPanel's "What is AR here?" box, CamerasPanel's footer sentence, ElementFxInspector's Animation-section explainer, NdiPanel's PROGRAM/TEST BARS paragraph (moved to button tooltips), SetInspector's AR Backplate blurb, ShapesPanel's footer, TemplatesPanel's footer + "saved from the Layers panel" sub-label, ImagePickerDialog's empty-state sentence. Verified via `grep -rn "leading-relaxed"` across `src/components` — only the legitimate `ComingSoonPanel` (an actual "not built yet" placeholder) and an unrelated shadcn utility class remain.

**Verified:** `tsc --noEmit` clean, `cargo check` clean (new MIME arms only), `scripts/verify-phase5.ts` still fully passing (Asset.family is optional — no round-trip regression), cold boot healthy with the persisted project loading through the extended schema.

**Follow-up fix (operator-reported, same day): Timeline sequencer was hijacking Play In/Out.** Symptom: "some graphics enter Program/Preview the instant an asset is added, others have no way of getting there at all." Root cause found by reading `TimelinePanel.tsx`'s sync effect end to end: every layer in a scene renders a ghost default clip `[0, 5]` (`sequence.ts`'s `DEFAULT_CLIP`) so it *can* be dragged onto the timeline — but the sync effect fired `playIn`/`playOut`/`resetPlayback` off that ghost the moment the effect ran, with no check for whether the operator had ever actually placed the layer on the timeline. Since the playhead starts at `0` and the ghost's `inTime` is `0`, simply **opening the Timeline workspace page fired `playIn` for every layer in the active scene instantly** — explaining "enters immediately." Scrubbing past the ghost's `outTime` (5s), or pressing Stop, then fired `playOut`/`resetPlayback` on those same layers — silently overriding whatever the operator had set by hand via Play In/Out in the Layers panel, explaining "no way of adding them." One bug, two symptoms, both purely about *when the operator had visited Timeline and where the playhead happened to be* — nothing to do with the asset system itself.

**Fix:** `sequence.ts` gained `hasClip(sceneId, layerId)` — true only once `setClip` has actually been called for that layer (real persisted entry), as opposed to `getClip`'s display-only fallback. `TimelinePanel`'s sync effect, `stop()`, and the "live" clip highlight now all gate on `hasClip`, so a layer never dragged onto the timeline is fully invisible to the sequencer and stays under manual Play In/Out control exactly as authored. Ghost (unsequenced) clips now render visually distinct — dashed, low-opacity, "not on the timeline yet, drag to sequence" — so this ambiguity can't recur silently. Regression-guarded in `scripts/verify-phase5.ts` section [9]: asserts the ghost clip *would* satisfy the old buggy fire condition (proving the bug scenario is real) and that the gated path never fires for an unsequenced layer. `tsc`/verify/cold-boot all clean.

## Phase 5.10 — Real Audio for Video/Live Sources — BUILT (2026-07-07)

Operator direction: "we need audio" — on both imported video clips and live sources.

**Data model:** `VideoElement` (2D) and `VideoFeedNode` (3D) each gained `volume?: number` (0-1, default 1) and `muted?: boolean` (default false — audible by default once on Program, matching vMix/OBS convention that sources play unless explicitly muted). Mirrored in schema, copied explicitly in both factory functions (`createVideoElement`/`createVideoFeedNode`) — this project has hit the "factory forgot to copy a new field" bug twice before (`scrollSpeed`, `gradient`), so both new fields are now regression-guarded in `scripts/verify-phase5.ts` with explicit round-trip checks, not just the aggregate one.

**Real capture:** `videoFeeds.ts`'s `getUserMedia`/`getDisplayMedia` calls now request `audio: true` (were `audio: false`) — a real mic track for capture devices, real system/tab audio for screen share (subject to the operator granting it in the browser's share dialog). URL/clip sources already carry their native encoded audio track. Every source element still opens `muted=true` at creation (required for autoplay without a user gesture); flipping `.muted` on an already-playing element afterward is allowed by Chromium's autoplay policy, so consumers unmute it after the fact based on authored prefs — no gesture-blocked audio.

**The `audible` gate — audio only plays in the Program window.** Threaded as an explicit prop through the whole render chain (`RenderOptions.audible` in `renderNodes.tsx`, `SetNodeContext.audible` in `SetNodes.tsx`, `DocumentRendererProps.audible`, `Set3dRenderer`'s `audible` prop) so the *visual* gating this project already uses everywhere (`interactive`) got a real audio counterpart instead of overloading it. `ProgramView` is the only caller that passes `audible` — the editor (`GfxEditor`'s SET backdrop, `Set3dEditor`), `PreviewView`, and the Show workspace's embedded `MonitorsPanel` (both PROGRAM and PREVIEW tiles) all stay silent by explicit design, each with an inline comment stating why: authoring must never blast live audio, and the embedded multiviewer would otherwise double/echo audio against the standalone Program window if both were open at once — a real risk caught by tracing the actual window-isolation architecture (Program/Preview are separate Tauri windows/JS realms; `videoFeeds.ts`'s cache is already per-window) before writing any code.

**One correctness bug caught before it shipped, not after:** confidence monitors (`ConfidenceMonitorView` in `SetNodes.tsx`) recursively mount the REAL target scene's node graph inside a render-texture — not a flat image. If a monitored scene contains its own live videofeed with a mic, blindly spreading `...ctx` into the nested context would have inherited `audible` and produced a SECOND independent audio element for the same physical mic — a real echo. Fixed by forcing `audible: false` unconditionally in the nested context, with the reasoning recorded inline: a confidence monitor is a picture of what's on air, never a second on-air audio path. AR backplates (`ArBackplateView`) were already isolated from this by construction (no `ctx`/audio wiring at all) — confirmed, not assumed.

**UI:** new shared `AudioControls.tsx` (mute toggle + volume slider, 0-100%) wired into both Inspectors — the 2D video-element section (`InspectorPanel.tsx`) and the 3D videofeed-node section (`SetInspector.tsx`, its own collapsible "Audio" section) — so the two can't drift from each other.

**Honest scope boundary — this does NOT yet reach NDI or OBS.** Confirmed by reading the actual Rust before writing anything: `ndi.rs`'s `NdiOutput::send_frame(frame, width, height, fps_n, fps_d)` takes video bytes only — no audio parameter exists in the trait, so NDI output is video-only today regardless of anything built here (the `clock_audio: true` seen in the sender config is an NDI SDK clocking flag, not an actual audio stream). Separately, OBS Browser Source still points at the Rust-rendered static `/program` HTML (rect/text only, no video/audio at all — the Phase 7 "serve the real renderer to OBS" item is still pending), so it doesn't receive this feature's audio either. What's real today: the standalone Program window itself now has genuine, controllable audio — useful for in-app monitoring and for any consumer that captures that window directly (e.g. OS-level audio loopback). Real NDI audio and real OBS audio both need the Phase 7 work to land first; noted as the natural continuation, not invented as already-done.

**Verified:** `tsc --noEmit` clean, `scripts/verify-phase5.ts` extended with explicit volume/muted round-trip checks for both the 2D element and the 3D node (passing), cold boot healthy.

## Phase 5.11 — Playout correctness fixes + 2D graphics arsenal + AI image key config — BUILT (2026-07-07)

**Playout correctness (operator-reported):**
- **HMR-duplicate-ticker fix.** The transport ticker in `playout.ts` is a module-level `setInterval`; a hot-reload of that module during dev left the OLD interval running alongside the new one, so a single 2-item schedule logged 4 as-run entries instead of 2. Fixed with `import.meta.hot.dispose()` clearing the interval on module replacement — a real bug, not a one-off fluke, since any edit to `playout.ts` during a dev session would have re-triggered it.
- **Live programs no longer auto-cut.** Per direct operator correction ("for live programs you wouldn't be cutting automatically when camera is on till control room ends the program"): a `type: "live"` item now HOLDS on air past its planned duration instead of auto-advancing — `endStatusFor()` treats live items as never "cut" by natural timeout, only by an explicit operator take (`next`/`previous`/`takeItem`), and those now log `completed` for a live item instead of `cut`. UI shows a `LIVE · HOLDS` badge, switches the countdown to `OVER +mm:ss` past the planned mark, and the Next card shows "on take" instead of a countdown while a live item is on air.

**2D graphics — group picking + a real shape arsenal (operator direction: "group picking for uniform moving... is very important" + "our 2D asset arsenal is too tiny"):**
- **Multi-select + grouping**, new in `GfxEditor.tsx`/`store.ts`: shift/ctrl-click adds to selection (same-layer only, to keep the Transformer/grouping coherent); a rubber-band marquee drag over empty canvas selects every element it overlaps. New store actions `groupElements`/`ungroupElement` — group's transform is the selection's bounding box, children rebased to group-relative coordinates (the Konva-container convention); ungroup lifts children back to absolute coordinates, folding in the group's own rotation. `Ctrl+G`/`Ctrl+Shift+G` plus toolbar buttons. Resizing a group via the Transformer now rescales its children's transforms too (previously they'd have snapped back to authored size on next render — caught before shipping by reasoning through what "bake Konva's scale into width/height" actually does for a group's *children*, not just the group node itself).
- **Shapes library** (`src/graphics/shapes.ts` + new `ShapesPanel.tsx`, tabbed into the Design workspace alongside Templates/Assets): 13 presets across Basic/Broadcast/Accents (panel, rounded, pill, circle, outline, divider, gloss bar, gradient panel, lower-third, badge, ribbon, corner accent, swatch) built from the same native rect/text/group primitives as everything else — fully editable after insert (recolor, gradient, skew, corners, text), multi-part presets pre-grouped so they move as one unit. Live thumbnail previews render the actual preset through the shared `renderNodes` pipeline, not a static image.
- **SVG upload** — `.svg` added to `IMAGE_EXTENSIONS` (frontend) and `image/svg+xml` added to the Rust `asset_mime()` table, so operators can bring their own vector graphics through the existing image-import path (ImagePickerDialog's file accept + the sidecar's asset serving). Researched and cited free CC0/public-domain SVG sources (SVG Repo, FreeSVG.org, Clipart.Free) for growing the built-in arsenal further without licensing risk.

**AI image generation — key configuration.** Discovered mid-session that a parallel work stream had already built AI image generation essentially end-to-end (`generate_ai_image_to_assets` in `lib.rs`, exposed both as a Tauri command and an axum `/assets/generate-image` route; full prompt/reference-image UI already in `AssetBrowserPanel.tsx`/`ImagePickerDialog.tsx` calling `generateAiImageAsset` in `assetImport.ts`, using OpenAI's `gpt-image-2`/`images.edits` for img2img via a pinned reference asset) — read it fully before touching anything to avoid building a duplicate, conflicting implementation. The one real gap: the API key was readable only from a process environment variable (`OPENAI_API_KEY`), with no way to configure it from inside the running app. Added: a Rust-only settings file (`ai_settings.json`, sibling to the assets dir) plus three new Tauri commands — `get_ai_settings_status` (returns only a `configured` boolean + optional model override, never the key), `set_openai_api_key`, `clear_openai_api_key`. `generate_ai_image_to_assets` now resolves the key/model from the stored file first, falling back to the env vars (so existing dev/CI usage keeps working). New `AiKeySettings.tsx` (collapsible, password-masked input, model override field) wired into `AssetBrowserPanel`'s Image tab — the key is write-only from the frontend's perspective and the actual OpenAI call happens entirely in Rust, so the key never sits in the web layer beyond the single save round-trip.

**Environment note:** `cargo fetch`/`cargo check` started failing with a Windows/schannel `CRYPT_E_NO_REVOCATION_CHECK` error partway through this session (an OCSP/CRL revocation-check failure reaching `static.crates.io`, unrelated to any code change). Fixed with the documented workaround — `check-revoke = false` under `[http]` in the user's `~/.cargo/config.toml` — which skips only the revocation-check step, not certificate validation itself.

**Verified:** `tsc --noEmit` clean, `cargo check` clean (including the new `reqwest`/`base64` deps already present from the parallel AI-image work).

## Phase 5.12 — NDI Tools Stage 1: real source discovery — BUILT (2026-07-08)

Operator downloaded and installed the NDI 6 Tools suite (Access Manager, Studio Monitor, Discovery Service, Test Patterns, etc. — separate standalone apps, not a library) and asked for an "NDI Tools panel in Settings," with an explicit instruction to research before implementing. Researched first: NDI Tools' own apps aren't embeddable; the real gap in our own code is that we only ever built NDI *send* (`ndi.rs`'s `DynamicNdi`) — there was no *receive/discovery* capability at all. Read the actual installed `Processing.NDI.Find.h`/`Recv.h` headers before writing anything. Proposed a two-stage plan (mirroring how Send itself was staged) and got explicit sign-off on Stage 1 only: real network discovery, no receive/preview yet.

**Backend:** `NdiOutput` trait gained `find_sources(timeout_ms) -> Result<Vec<NdiSourceInfo>, String>` (`NdiSourceInfo { name, url_address }`). `UnavailableNdiOutput` returns an honest `Err` (same discipline as every other method). `DynamicNdi` dynamically loads 4 more symbols (`NDIlib_find_create_v2/_destroy/_wait_for_sources/_get_current_sources`) via the same per-symbol `libloading` pattern the sender already uses, with new `NdiFindCreateT`/`NdiSourceT` `repr(C)` structs matching the SDK headers exactly (verified field-for-field against the installed headers, not guessed). Creates a fresh finder per call (this backs a manual/auto "refresh" UI action, not a hot path — no persistent finder state to manage) and copies every `char*` into an owned `String` before destroying the finder, since the SDK documents those buffers as invalid after `_destroy`. New Tauri command `list_ndi_sources(timeoutMs?)`.

**Frontend — a real Settings surface, which didn't exist before:** new `SettingsDialog` (gear icon in `PersistentShell`, reachable from every workspace) housing the first real section, `NdiToolsSettings` — auto-refreshing (4s) + manual-refresh live list of actual discovered NDI sources, empty state is an honest "no sources found" (never a fake placeholder row), and each row is explicitly labeled "discover only" since receive isn't built. The panel states outright that Stage 2 (receive/preview/use-as-input) is not built yet — no implying more than what's real.

**Verified so far:** `cargo test` — all 5 `ndi::` unit tests pass (including a new `stub_find_sources_always_errs`). `cargo check` and `tsc --noEmit` both clean. **Live click-through (self-discovery: start our own NDI sender, then confirm `list_ndi_sources` independently finds it) is in progress, not yet confirmed** — recorded here once actually run, not before.

**Explicitly deferred (Stage 2, confirmed separate task, not started):** receiving a selected source (`NDIlib_recv_create_v3`/`_capture_v3`), decoding frames, and exposing it as a real `VideoSource` variant (e.g. `{type:"ndi", sourceName}`) usable anywhere a video source is accepted today (2D video element, 3D videofeed node) — likely via a JPEG-encoded MJPEG stream from the axum sidecar, since Konva's `<Image>` accepts any `CanvasImageSource` including a plain `<img>`.

## Phase 5.13 — Realistic PBR Pipeline — DONE (2026-07-08)

Companion: [`docs/REALISM_PIPELINE.md`](docs/REALISM_PIPELINE.md) (strategy **1C**: enriched offline IBL + hero planar + optional High-tier SSR).

**Shipped:**
- Document model: Physical/clearcoat/maps/`envMapIntensity` on `MaterialProps`; floor `reflector`; primitive `reflector` (desk); `qualityTier` / `planarReflection` / `envResolution` / `envCubemapAssetId` / `ssr` on `SetRenderSettings` — schema-mirrored + factory defaults (Med posture: env 128, planar on, SSR off).
- Gates in [`src/document/qualityTiers.ts`](src/document/qualityTiers.ts).
- Runtime: Standard/Physical materials; tiered Lightformer bake resolution; optional **local cubemap** IBL via `envCubemapAssetId`; **MeshReflectorMaterial** floor (Med+) and desk top (High + maxCount 2); **SSREffect** from `realism-effects` in `SetPostEffects` (High + explicit enable only); SetInspector quality-tier + cubemap picker + desk planar + SSR toggles.
- Verify: `scripts/verify-phase5.ts` section 11 — realism fields round-trip.

**Deferred:** cold-boot soak on min-spec hardware; EXR/cubemap6 asset kind (equirect image only today).

## Phase 6.1 — Virtual Camera Motion + Squads/Maps/Lighting packages — BUILT (2026-07-08)

Session driven from the operator's broadcast-AR direction brief (audit first, then: virtual camera system §5, squads/stats/maps §7, lights §8). Audit confirmed the app already covered most of the brief; the real gaps were camera *motion* (cuts existed, nothing else), formation/map graphics, and named lighting rigs. Main thread built the camera architecture + all shared-file integration; two Sonnet subagents delivered the isolated modules per MODEL_STRATEGY.md (a third stalled twice on the maps module — main thread wrote `maps.ts` itself rather than respawning a third time).

**Virtual camera motion engine (`src/document/cameraMoves.ts` + rigs in `Set3dRenderer.tsx`) — smooth takes, push/pull/slide, orbit/follow/focus, and PVW camera rehearsal.** State model deliberately copies `layerPlayback`'s proven contract: a transient, never-persisted slice (`cameraMoves`/`cameraOrbits`/`cameraPreview`, keyed by set3d layer id) pushed in the program envelope with a shared `startedAt` wall-clock timestamp, so Program, Preview, and the embedded monitors each independently reconstruct the identical mid-flight pose with no sync protocol — and the END state is always committed to the document first (`activeCameraId` on take, the camera node's transform on push/slide/focus/orbit-stop), so a consumer that misses the whole animation still lands on the correct final frame, and every automated move is one clean undo entry. Render-side, a transient rig camera (drei `PerspectiveCamera makeDefault`) mounts AFTER the node graph while a move/orbit is live — its makeDefault wins, unmounting restores the committed node camera — and reads the *destination's live world pose* off the scene graph by object name (`camnode-<id>`/`setnode-<id>`, names added in SetNodes.tsx), so landings are exact even for cameras nested in transformed groups. Orbit pivots on the live world position of the subject node (follow = orbit at 0°/s; focus = lookAt-rotation commit + smooth move; ease via `gsap.parseEase`, same convention as timelines). `CamerasPanel` grew the operator surface: per-camera **PVW** (rehearse a shot in the Preview window only — new `role`/`cameraPreview` threading through DocumentRenderer, honored only by preview-role renders) / **TAKE** (smooth, with a panel-level duration slider + SMOOTH/CINE/LINEAR ease buttons) / **CUT** (instant, unchanged semantics), plus a per-set "Camera moves" bar (PUSH IN / PULL BACK / SLIDE ◀▶ / ORBIT / FOLLOW / FOCUS on the selected node, with honest disabled-state tooltips). No schema/factory changes were needed — nothing new persists inside `project` (camera nodes themselves were already the persisted, renameable shot presets).

**One real bug caught by the verify suite before it shipped:** `orbitPosition` originally derived the camera's offset against the *current* pivot each frame, so a follow shot on a moving subject silently re-derived its radius instead of holding its framing. Fixed by capturing `pivotStart` on the orbit entry (offset = from − pivotStart, carried along by the live pivot); regression-checked in section [13] ("follow (0°/s): original offset held on a moving pivot").

**Squad package (`src/sports/squads.ts`, Sonnet-built to CONVENTIONS.md/fullscreens.ts patterns, main-thread reviewed):** `SQUAD_KEYS`/`SQUAD_DEFAULTS` (team/formation/coach + name/number for all 11 starters, live-editable as the new `squad` source), 4 formations (4-3-3/4-4-2/3-5-2/4-2-3-1) as normalized pitch coordinates, `createFormationBoard()` (full-screen gradient pitch with real line markings, 11 cascading player markers — Brand-Kit-bound circles, bound numbers/names — plus a skewed-gloss team/formation/coach stack) and `createPlayerCard()` (lower-third spotlight with headshot image slot). One compile bug found in review (undefined `W`/`H` consts) — fixed at integration.

**Map package (`src/graphics/maps.ts`, main-thread-built after the subagent stalled):** offline-honest design — no tile servers on this network, so the operator drops their own map artwork into a real image slot and the template supplies the broadcast chrome: header/subtitle straps, 4 pulsing pins (`ElementAnim.loop`) with bound label/value tabs (`map.*` source), accent frame, legend. `createMapPin()` exported for extra pins.

**Lighting system (`src/sets/lightingPresets.ts` + `LightingPanel.tsx`, Sonnet-built, reviewed):** `kelvinToHex` (Tanner Helland black-body), 5 named rigs (Studio Standard / News Desk / Sports Arena / Dramatic / Softbox Interview) built through `createLightNode` with the studioSets lookAt-aim convention, key-only shadows, per-rig color temperatures; the panel gives per-light control (intensity, color, a write-only kelvin slider, XYZ, castShadow), card-grid rig apply (replaces the set's lights), and **operator-saved lighting presets** persisted in the existing `app_state` k/v table (`user_lighting_presets`, fresh node ids on apply) — no schema migration needed. Registered in the Studio workspace tabbed with Cameras (layout key → v3).

**Integration (main thread, shared files):** `squad`+`map` added to `FeedId`/`FEED_IDS`/state (Data Sources panel picks both up automatically), Templates panel grew a "Maps" category + 4 squad-board cards + player-spotlight card (real live-data previews like everything else), envelope + push subscriptions extended in `persistence.ts`/`useDocumentEnvelope.ts`, `ProgramView`/`PreviewView`/`MonitorsPanel` thread the camera state with correct roles.

**Also fixed (a parallel session's in-progress realism/SSR work was breaking the build, wired per author intent, not deleted):** `src/shims/three.ts`'s `three-original` vite-alias had no TS resolution — new `src/shims/modules.d.ts` declares `three-original` (re-exporting three's real types) and untyped `realism-effects`; the legacy `.texture`-returns-array MRT getter moved to `Object.defineProperty` (three's types declare `texture` as a plain property, which accessor syntax can't legally override — TS2610).

**Verified:** `scripts/verify-phase5.ts` extended with section [13] (squad boards ×4: bound keys resolve against the real flattened sources, 11 markers, in-frame, timelines; player card + map board incl. unfilled-slot and loop-pulse checks; camera slice exercised against the REAL `useDocStore` — take/cut/push/focus/orbit/preview, focus verified by actual −Z·subject dot product, follow verified on a moving pivot) — **ALL CHECKS PASSED**. Full `tsc --noEmit` pass over the integrated tree. **Pending operator eyes:** the actual motion feel of TAKE/orbit in the running app (Program/Preview windows), the Lighting panel look, and filling real map artwork/headshots into the new slots.

## Phase 6.2 — Sports AR 3D Models Library (10 models + data manifest system) — BUILT & VERIFIED (2026-07-11)

The `AR 3D Models > Sports Graphics` library: ten independent, parametric, GLB-backed AR panel models built from the operator's 10 reference images (`src/ar-engine/sportsPanels/`), each opening EMPTY and sport-neutral (no team/score/logo/words/colour identity — regression-guarded), all binding to a new provider-neutral `sports.*` live feed. Companion doc: [`docs/SPORTS_AR_MODELS.md`](docs/SPORTS_AR_MODELS.md).

**Engine extensions (all schema-mirrored + factory-copied — the twice-bitten field-drop class):** `prism` primitive (extruded convex polygon, optional hole ring, real inward bevels — `prismGeometry.ts`, rendered in SetNodes/ArPanelView, exported to GLB); `arModel` group metadata (modelId+version+params → parametric rebuild + "Reset Geometry to Reference" preserving zone mappings); `visibilityRule` (declarative data-driven show/hide, baked into `visible` at output push); `updateAnim` (pulse/flash/fade on RESOLVED-value change only — re-received identical data never re-animates); `arPlacement` (cameraFacing billboard, presenter/player node-anchoring, screenSpace camera-ride — real per-frame behaviours, not metadata); named binding formatters (`ar-system/binding/format.ts`, shared by editor render AND output bake so they can't drift).

**Distribution assets** (`public/assets/ar/sports/`, generated from the registry by `scripts/generate-sports-ar-assets.ts` so manifests can't drift from code): library manifest listing all 10, 10 per-model manifests, 10 real binary GLBs (84–169KB, exported via three's GLTFExporter from the same geometry the renderer draws; unfilled branding slots export as named empties, never fake dark plates), `sports-live-data.schema.json` (+ per-sport `extensions` so base manifests never change per sport), `sports-data-sources.manifest.json` (no secrets — credential-store note inline), 10 real-geometry thumbnails (rendered headlessly through `thumbnails.html` + `scripts/capture-sports-ar-thumbnails.mjs`).

**Data path:** `sports` feed (all-empty defaults) in dataSources; `sportsConnector.ts` ingests schema-shaped JSON payloads / WebSocket / REST polling / test simulator with fallback-based validation (invalid scores/colours/clocks/URLs dropped with editor warnings, never a crash). **One real perf lesson:** per-field `setFeedValue` ingest = one full output bake each (a 25-field payload visibly janked the app) — fixed with a batched `mergeFeedValues` (one store update per payload); combined with the parallel session's `buildDataValuesCached`/per-key-subscription refactor, ingest went 39s → 0.7s measured live.

**UI:** Build tab hosts the `AR 3D Models › Sports Graphics` card grid (real snapshot previews, badges, insert-on-click); new **Model** tab (`SportsModelInspector`) — 17 geometry params + reset, 6 colour groups (picker/hex/recent/apply-to-group/reset-to-neutral), 10 neutral material presets, placement modes, per-zone Data Mapping table (source/formatter/fallback/visibility rule/update animation), data-source connectors, performance tier, optional prop modules (disabled by default under OPTIONAL_SPORT_PROPS), Save Variant (config-only, GLB never duplicated), Export GLB.

**Verified:** `scripts/verify-sports-ar-models.ts` — **511 checks, 0 failures** (hierarchy ×10, empty/neutral defaults ×10, real prism/bevel/ring geometry ×10, value-identical Zod round-trip, rebuild/reset preserving mappings, formatters, visibility ops, ingest validation, manifest↔registry byte-consistency, GLB magic+size). **Live via CDP on cold boots:** library grid renders with all 10 real previews; Panels 01/02/03 inserted as independent instances (full mandated hierarchy in the outliner); scoreHome mapped → simulator data baked `"1"` into the output envelope; width 3.5 rebuild + Reset-to-Reference both reached the wire; emissive-group recolour hit every strip; mapping/updateAnim/colour/width all confirmed in the RAW SQLite doc across page reload AND full process restart; Load PVW + Take put the panel scene on Programme (programSceneId confirmed, `/program` serving, Show-page PGM/PVW monitors rendering with zero lost WebGL contexts).

**Environment incident, recorded honestly:** mid-verification the machine (8GB RAM) ran out of commit — the Rust host died on a 1MB allocation (`memory allocation failed` → `STATUS_STACK_BUFFER_OVERRUN`) and one relaunch produced a windowless app (WebView2 browser-process creation failed silently). Contributors: ~31 orphaned `msedgewebview2.exe` from prior sessions (the known Phase 2 gotcha) plus multi-day-old dev processes. Thumbnails were captured via headless Playwright against the running vite server instead of the app for this reason. A zombie webview held the CDP port with 0 handles — unkillable until it expired; using a different `--remote-debugging-port` per session sidesteps it.

## Phase 6 — AR + Maps + Charts

Camera backplate, FreeD-ready virtual camera, MapLibre, visx chart builder (as `map`/`chart` layers, also already reserved). Phase 6.1 above delivered the practical offline map board and the full virtual-camera motion layer; remaining here: live tile maps (MapLibre, needs network), visx charts, FreeD physical tracking.

## Phase 7 — Control + automation — COMPLETE (2026-07-11)

Rust control server, Bitfocus Companion module, rundown runner (import/export), FFmpeg record, Spout stub. *DoD:* Broadcast control via external HTTP+SSE; Companion buttons drive the Control Room; rundown load/save; record starts/stops. ✅ Complete: all DoD met except live Companion-in-hand click-through (deferred to operator pass on Windows).

**Transport — HTTP+SSE, not raw WebSockets — designed by Opus, implemented by Sonnet/Haiku hybrid, verification by Sonnet.** Full rationale in [`docs/PHASE7_DESIGN.md`](docs/PHASE7_DESIGN.md). The short version: every Phase 7 message is one-directional (client→server = command, server→client = state), so full duplex WS buys nothing over a well-designed POST + SSE pair — and the Cargo.toml already documents that `tokio-tungstenite` failed crates.io TLS revocation on this project's build network. Adding WS just to check a box in the handoff brief would have introduced a real build risk with zero functional gain. Bitfocus Companion, vMix, and ATEM all ship HTTP-based modules — this is a well-precedented choice, not a corner-cut.

**Rust control server (`src-tauri/src/control_server.rs`, ~150 lines):** two axum routes bolted onto the existing sidecar via a sub-router (`Router::merge`) so the Phase 2 `AppState`-typed routes stay unchanged. `POST /control/command` accepts a `{seq, type, params}` JSON envelope, validates `type` against a compile-time `KNOWN_COMMANDS` list (an obviously-wrong `{"type":"foo"}` gets a synchronous HTTP 400, not a silent no-op); Rust-side commands (`startRecord`/`stopRecord`/`ping`) dispatch inline, everything else emits a `control:command` Tauri event to the Control Room window. `GET /control/state/stream` reuses the exact Phase 2 SSE + tokio broadcast pattern (initial-frame from a mutex-guarded buffer, live updates via `BroadcastStream`) so late joiners immediately get a real snapshot — same idempotency and lag semantics as `/document/stream`.

**Bridge (`src/document/controlBridge.ts`, ~200 lines):** mounted only in `ControlRoomView` so Program/Preview windows can never double-emit. Subscribes to `useDocStore` + `usePlayoutStore` and coalesces sub-frame mutations into one push per `requestAnimationFrame` (proven-real-time for the 60Hz store activity Phase 3 introduced); polls `get_record_status` + `get_ndi_status` every second for slices that live in Rust. Incoming commands route through a `switch` with a `never`-typed exhaustiveness check — adding a new `ControlCommandType` and forgetting to handle it here is a compile error. The `ControlStateSnapshot` type is the single source of truth: the Rust server never parses it (it forwards strings verbatim), the Companion module reads it, `verify-phase7.ts` cross-checks the vocabulary — one shape, four consumers.

**Bitfocus Companion module (`src/companion-module/`) — standalone Node 22 ESM package.** `manifest.json` + `package.json` follow Companion 3.5's plugin format (`@companion-module/base` runtime, `nodejs-ipc` API); `index.js` implements the standard `InstanceBase` with 13 actions matching the wire vocabulary, 4 feedbacks (on-air, recording, schedule playing, NDI streaming), 13 live variables, and 6 starter presets (Take/Next/Previous/Play/Pause/Record). The SSE consumer is a minimal hand-rolled parser (splits on `\n\n`, keeps `data:` lines) — deliberately not depending on `eventsource` since Node 22 has fetch built in and dropping a dependency shrinks the operator install surface. Reconnect on disconnect (2s backoff). Not pre-integrated with a Companion installation — the module is source only; operators drop it into Companion's user-modules folder, per the standard "Import module" flow every third-party Companion module uses. Documented in [`src/companion-module/README.md`](src/companion-module/README.md).

**Rundown import/export (extends `playout.ts`, ~90 new lines):** CSV parser is a minimal RFC 4180 subset (handles quoted cells with embedded commas + `""` escapes, both `\r\n` and `\n`) — deliberately not a general-purpose parser, since the input surface is only ever this project's own exports or a spreadsheet-authored rundown. Columns matched by header name (case-insensitive, order-tolerant); `duration` parses bare seconds *or* `mm:ss` *or* `hh:mm:ss`; unknown `type` values fall back to `"program"` (never silently coerced to an invalid enum). JSON export/import round-trips native shapes (keeps `sceneId` verbatim, no name-lookup indirection). `replaceRundown` action correctly `stop()`s if the currently-airing item won't survive the import — otherwise the ticker would advance a ghost `currentId` that no longer resolves.

**FFmpeg record (`src-tauri/src/record.rs`, ~250 lines including 5 unit tests):** one active record at a time, guarded by a `Mutex<Option<Child>>`. Per-OS screen-capture backends (`gdigrab` on Windows, `avfoundation` on macOS, `x11grab` on Linux) with H.264/ProRes/DNxHD codec paths; output file names carry a ms timestamp so operator collisions can't overwrite. Graceful stop sends `q\n` to ffmpeg's stdin (the documented graceful-quit for capture inputs), falls back to `kill()` if it doesn't take within 4s. `try_wait()` on `get_record_status` catches silent subprocess exits so the UI can't lie about being still-recording after ffmpeg crashed. Honest failure modes: ffmpeg not on PATH → error surfaced to `RecordStatus.last_error`; already recording → refused (operator must stop first). **Unit tests verified live** (`cargo test --lib record`): 5/5 pass, covering arg construction, codec/ext mapping, and filename sanitization.

**Spout/Syphon (`src-tauri/src/spout.rs`):** stubbed with an honest `available: false` + a per-platform `reason` explaining that real GPU texture sharing is Phase 8 work. The Tauri command surface (`get_spout_status`) matches the NDI pattern from Phase 2 so Phase 8's real dynamic-load implementation can swap the backend without touching the frontend.

**PlayoutPanel UI extensions:** transport bar gains a Record button (live-polled state from Rust, error surfaced inline with dismiss); rundown tab gains Import (CSV/JSON, replaces current items after cleanly stopping any ghost `currentId`), Export CSV, Export JSON. All wired to the pure functions in `playout.ts` — no logic in the panel.

**Verified (2026-07-11):** `bunx tsc --noEmit` clean (exit 0); `bun run build` succeeds (production bundle, 20s); `bun run scripts/verify-phase7.ts` passes all 13 assertions covering CSV/JSON round-trip, edge cases (blank titles, `mm:ss`/`hh:mm:ss` durations, quoted cells with commas, unknown types), and the control-vocabulary contract shared with the Companion module; `cargo check --tests` clean; `cargo test --lib record` passes 5/5 unit tests. Real ffmpeg subprocess spawn, real Companion-device button press, and real SSE snapshot arrival at a remote Companion install are operator-pass work on Windows (unchanged blocker from Phases 3–6 — this dev environment isn't a registered Windows app `computer-use` can target).

**Explicitly out of scope this pass (deferred to Phase 8):**
- Real Spout/Syphon (Windows GPU texture sharing) — needs the same dynamic-load runway as NDI Stage 1, done there but not repeated here.
- Push-driven OBS updates — `/program`'s Rust-rendered snapshot stays the Phase 1 static render (matches existing precedent).
- Automation scripting (conditional logic, rundown.cloud connectors) — Phase 8+ work per the handoff brief's explicit note.

## Phase 8 — Hardening

## Phase 8 — Hardening — IN PROGRESS (2026-07-11)

1080p60 zero-drop performance, packaging, signed Windows installer. This pass delivers the honest cut of Phase 8 that's *end-to-end verifiable from this Linux environment*: automated CI, cross-platform bundle configuration, and expanded verification harness. Real Spout/Syphon + Windows-signed installer + live 1080p60 profiling are documented deferrals — they need Windows/broadcast-rig sessions to ship honestly. Design in [`docs/PHASE8_DESIGN.md`](docs/PHASE8_DESIGN.md).

**Automated CI (`.github/workflows/ci.yml`) — the "no more silent regressions" guard rail.** Two parallel jobs on every push and PR: `verify-frontend` runs `bun install --frozen-lockfile → bunx tsc --noEmit → bun run build → bun run scripts/verify-phase7.ts → bun run scripts/verify-phase8.ts`, and `verify-rust` installs the exact Ubuntu 24.04 GTK/WebKit dev packages that Phase 7 verification identified as needed (this session hit and fixed the `gdk-3.0.pc not found` failure live) and runs `cargo check --tests + cargo test --lib`. Bun and cargo caches keyed by lockfile hash so warm CI is fast. Concurrency-cancelled per branch so stale runs don't block feedback. Deliberate omissions documented: no Windows/macOS bundle build in CI (a broken bundle config fails `tauri build` locally before push), no ESLint (project doesn't have one, and adding one is a project-shape decision not hardening), no coverage numbers (they invite gaming on this project's mix of pure transforms + Konva rendering).

**Tauri bundle config (`src-tauri/tauri.conf.json`) — full cross-platform installer metadata.** Publisher, homepage, category, copyright, short + long descriptions land in every platform's installer chrome and its "Programs and Features"/"Applications" listing. Windows: `wix.upgradeCode` is a fixed UUID so future installers replace-in-place (never side-install a second copy — a real broadcast-rig operator pain if you get this wrong), `nsis.installMode: "perMachine"` (broadcast rigs are shared, not per-user), `digestAlgorithm: sha256` + `timestampUrl` populated so signed installers timestamp correctly, `certificateThumbprint` env-driven so a real signing cert is attached at build time without a secret ever touching the repo. macOS: `minimumSystemVersion: "11.0"` (Big Sur — the oldest with Metal-first WebView), `signingIdentity` env-driven. Linux: `deb.depends` pins `libgtk-3-0t64 | libgtk-3-0`, `libwebkit2gtk-4.1-0`, `libayatana-appindicator3-1` — package names verified against Ubuntu 24.04 (the runtime that this session's `cargo check` actually validated against, not guessed). AppImage bundles its own deps so no pins needed there.

**Verification harness expansion (`scripts/verify-phase8.ts`, 20 tests all passing) — pins the invariants Phase 7 shipped but only sanity-tested:**
- **Control protocol wire contract:** `ControlCommand` and `ControlStateSnapshot` round-trip through `JSON.stringify → JSON.parse` without field loss. Catches the schema-drift regression pattern this project's prior audits (Phase 2, Phase 3) explicitly flagged.
- **Playout HOLD semantics for live items:** `endStatusFor` (freshly exported for this test) is proven correct at t=0, mid-item, at planned end, AND at 2× planned duration — the operator-visible property that a live-camera take *never* registers as a cut in the as-run log. If this ever regresses to `"cut"`, it lies in the compliance/billing export.
- **`projectedStartSecs` math with wraparound:** stacking + midnight-crossing (23:30 + 3600s → 00:30 next day) + exact-midnight (23:30 + 1800s → 00:00). One real bug caught in this session: my first test data was wrong (23:30 + 1800 = 00:00, not 00:30), the test failed exactly as it should, then I corrected the test — real signal, not typo.
- **Sport-schema binding integrity, all 8 sports:** every scorebug is a `gfx2d` layer with bindings covering the mandatory floor from `CONVENTIONS.md` (`<sport>.homeTeam`/`awayTeam`/`homeScore`/`awayScore`/`clock`/`period`). Any of the 8 losing a floor binding is instant test failure.
- **Rundown CSV/JSON 3-hop idempotence:** import → export → import → export, verify the second CSV text equals the first, verify meaningful fields (title/type/duration/sceneId) are stable through JSON hops too.

**Rust unit tests expanded** (17/17 total passing across `record`/`control_server`/`ndi`/`status`): `known_commands_contains_every_documented_command` pins the wire vocabulary against the design doc so a rename would fail CI; `is_rust_command_covers_rust_side_dispatch` + `is_rust_command_rejects_js_side_commands` pin the routing invariant that mutation commands route through the JS bridge (never inline in Rust — otherwise the Zustand store never reflects the change).

**Verified (2026-07-11):** `bunx tsc --noEmit` exit 0; `bun run build` succeeds (19s); `bun run scripts/verify-phase7.ts` PASS; `bun run scripts/verify-phase8.ts` PASS (all 20 assertions); `cargo test --lib` PASS (17/17); tauri.conf.json parses as valid JSON. The CI workflow itself will run for the first time on the PR that lands it.

**Explicitly deferred (need Windows/broadcast-rig sessions):**
- Real Spout/Syphon dynamic-load (`spout.rs` stays the honest-unavailable stub from Phase 7; NDI Stage 1's pattern is proven and this ports mechanically when a Windows session is available).
- Signed installers (config is here; attaching a cert is one flag at build time).
- Live 1080p60 profiling (needs a running Windows rig to profile against — Phase 2's frame-decode tuning was justified by real measurements, not guesses).

## Phase 9 — Rundown Studio connector — COMPLETE (2026-07-11)

External rundown-system integration: import cues from **Rundown Studio** (`rundownstudio.app`) into our playout. ✅ Complete: all DoD met except live token round-trip (needs a real Rundown Studio subscription to verify end-to-end).

**Product rename — Rundown Studio, not "rundown.cloud".** The Phase 7 handoff brief said "rundown.cloud", but there's no product by that name. The real thing is Rundown Studio, which has a proper OpenAPI 3.0 spec (fetched live in this session: `/api-v0/docs/spec.json`, 40KB, 35 endpoints), Bearer-token auth, standard rate limiting, and already ships a Bitfocus Companion module — so we're integrating against the exact operator base whose Companion setup Phase 7 targets. Design in [`docs/PHASE9_DESIGN.md`](docs/PHASE9_DESIGN.md).

**Rust connector (`src-tauri/src/rundowncloud.rs`, ~200 lines + 4 unit tests):** six Tauri commands — `get_rundowncloud_status`, `set_rundowncloud_config`, `clear_rundowncloud_config`, `ping_rundowncloud`, `fetch_rundowncloud_rundown`, `fetch_rundowncloud_cues`. Settings persist in `rundowncloud_settings.json` alongside `ai_settings.json` (the Phase 6.4 AI image gen precedent — parent-of-assets-dir so secrets never touch the axum-served path). API token never leaves Rust in plaintext; the status endpoint returns `configured: bool` only. Rundown ID validated as `^[a-zA-Z0-9]{20}$` client-side before every HTTP call so a mistyped ID gets a clear message instead of a server 400. Rundown/Cue schemas mirror the OpenAPI spec exactly (camelCase serde, `#[serde(default)]` on optional timestamps so a shape change from RS's side doesn't crash the parse). Bearer auth via `reqwest`'s `bearer_auth()` — token never appears in URLs or logs.

**Pure mapping (`src/document/rundowncloud.ts`'s `mapCueToItem`):** deliberately side-effect-free so `verify-phase9.ts` can pin the mapping without spinning up a store or the Tauri IPC layer. Documented decisions: ms→s conversion clamped to a minimum of 1s (the playout ticker will divide-by-zero otherwise); title-based type inference with `\blive\b` regex (word-boundary, so "delivery"/"alive" don't false-positive as live cues); `sceneId` always null on import (operator assigns after); local ids regenerated (no round-trip contract with RS's Cue ids). Empty/whitespace titles fall back to `"Untitled cue"`.

**PlayoutPanel UI:** compact "Rundown Studio" strip below the record-error banner. Not-configured: single "Configure" button. Configured: shows the rundown id, Ping (validates token vs `/ping`), Info (fetches rundown metadata, shows name + status inline), **Import cues** (fetches, maps, calls `replaceRundown` — Phase 7's ghost-currentId guard still applies), clear. Config dialog is a real password-input for the token, 20-char maxLength on the rundown-id field, Save disabled until both non-empty and id-length correct. Errors surfaced inline with dismiss.

**Verified (2026-07-11):**
- `bunx tsc --noEmit` exit 0
- `bun run build` succeeds (17s)
- `bun run scripts/verify-phase9.ts` — **22/22 pass** covering ms→s clamps, title normalization, `\blive\b` word-boundary matching, sceneId invariant, batch ordering, unique-id generation
- `cargo check --tests` clean
- `cargo test --lib` — **21/21 pass** (17 previous + 4 new rundowncloud tests)
- CI workflow extended to run verify-phase9 alongside 7/8

**Not verified in this pass:** live token round-trip against a real Rundown Studio account. No way to script this without a valid subscription; recommend an operator pass — configure a token in the panel, hit Ping (expect 200), hit Import cues, confirm items appear in the rundown tab.

**Explicit deferrals (Phase 9.1+):** live Socket.io state sync from Rundown Studio → our AS-RUN log; two-way control (sending start/pause/next back to their runtime); columns / mentions / text-variables integration for lower-third data bindings. All three raise the "who's in control" question and want their own design pass.

## Phase 10 — MOS teleprompter + Automation scripting — COMPLETE (2026-07-11)

Two independent Stage 1 deliverables in one PR: **MOS Protocol 2.8.5 subset** (rundown import from iNews/ENPS/Octopus over TCP) and **an in-app automation scripting engine** (trigger → optional condition → action, gated by a hard rate limit + operator kill switch). Both share PlayoutPanel/AutomationPanel surfaces, and automation rules can compose with rundown-item transitions that arrive over MOS or manually. Design in [`docs/PHASE10_DESIGN.md`](docs/PHASE10_DESIGN.md).

**MOS Protocol Stage 1 (`src-tauri/src/mos.rs`, ~450 lines + 9 unit tests):** hand-rolled two-pass XML parser over `quick-xml`'s streaming reader — first pass records every `(path, text)` pair, second pass extracts fields by whitelisted path. Chosen over serde's macro derive because MOS messages have real dialect variance across NRCS vendors and a positional post-parse fixup (e.g. `roStoryInsert`'s first `<storyID>` = target, subsequent ones = payload) is impossible to express as serde annotations. Handles: `heartbeat`, `mosID`, `roCreate` (full rundown), `roStorySend` (single-story update), `roStoryDelete`/`Insert`/`Move`, `roDelete`. Unknown roles yield `MosMessage::Unhandled { role_name, message_id }` — never a panic, never a silent drop. `build_heartbeat_ack()` emits real MOS-wire-format XML with the `\x00` message terminator NRCS expects. Persisted config in `mos_settings.json` alongside the other feature-specific settings; Tauri commands `get_mos_status` + `set_mos_config` (listen port + our MOS ID + optional expected NCS MOS ID + enable flag). **The TCP listen loop itself is deferred to Stage 2** — this pass ships the parser, config surface, and heartbeat builder so a Stage 2 spawner is drop-in.

**Automation scripting engine (`src/document/automation.ts`, ~250 lines):** rules are `{trigger, optional condition, action}`. Fixed trigger vocabulary (`on_take` / `on_item_start` / `on_item_end` / `on_timer(seconds)`), fixed action vocabulary (must be one of Phase 7's 13 `ControlCommandType` values), single-condition eval against a whitelisted set of `ControlStateSnapshot` field names with safe operators only (`== != > < >= <=`). Explicitly not Turing-complete — no loops, no user-defined functions, no dynamic key access. Rate limiter caps the whole engine at **10 actions per rolling 1s**; exceeding it puts the engine into a rateLimited state that stops all firing and surfaces a red banner until the operator clicks Resume. Master enable/disable is a persisted toggle, evaluated before any rule fires. Manual takes always win because rule-generated actions and Companion button presses route through the same `dispatchCommand` path — a store subscription that fires during a manual mutation still evaluates against the post-mutation snapshot.

**Bridge integration (`controlBridge.ts`):** on_take / on_item_start / on_item_end triggers detect transitions by comparing successive snapshots (tracked in effect-local closures — not stored in Zustand, since prior-value state is a pure engine internal). on_timer piggybacks on the existing 1s status poll rather than spinning a second timer. Condition evaluation flattens the nested `snapshot.recording`/`snapshot.ndi` objects into a single-level `Record<string, unknown>` so `recordingActive` and `ndiStreaming` are one lookup, not two.

**PlayoutPanel gains a MOS strip** below the Rundown Studio one; **AutomationPanel is a new dockview panel** in the Playout workspace (layout key bumped v1 → v2 to force redraw of persisted layouts). Two-pane rule editor: list on the left with per-rule enable toggles, editor on the right with trigger dropdown, optional-condition builder, action dropdown + JSON-blob params field for scene/layer/item ids. Master kill switch in the header, rate-limit banner with Resume button surfaces automatically when the engine trips.

**Verified (2026-07-11):**
- `bunx tsc --noEmit` exit 0
- `bun run build` succeeds (25s)
- `bun run scripts/verify-phase10.ts` — **25/25 pass** covering condition eval (every op × every scalar type + safe-fail on missing field + no string-to-number coercion), rule validation (empty name / unknown trigger / timer < 1s / unknown action / non-whitelist field), rate limit (10 allowed, 11th blocked, window rolls off, blocked action doesn't advance counter), timer scheduler (first-fire / before-interval / at-boundary / < 1s refused / multi-interval catch-up)
- `cargo test --lib` — **30/30 pass** (17 previous + 4 rundowncloud + 9 new mos)
- All previous verify-phase{7,8,9} still pass
- CI extended to run verify-phase10

**Not verified in this pass:** live NRCS interop. A signed MOS certification is a broadcast-industry process; Phase 10 delivers the wire format parsed correctly against public XML samples, not a certification claim. Real automation rule against a running Control Room is an operator pass on Windows.

**Explicitly deferred (Stage 2):** MOS TCP server spawn (parser + config are here; spawner is the only remaining piece); MOS outbound status reporting (`roReq`, `roStorySchedule`); bidirectional `mosObj*` asset sync; automation multi-action rules (do 3 things when a trigger fires); composable conditions (AND/OR chains); MOS-message trigger source for automation (`on_mos_message`) — composes naturally once both are landed, deferred because operator use cases aren't clear yet.

## Phase 10.1 — MOS TCP listener + automation composability — COMPLETE (2026-07-11)

Closes both open loops from Phase 10: **the MOS Stage 2 TCP server** so a real NRCS can push a rundown into BGE, and **automation composability** (multi-action rules + `on_mos_message` trigger) so the two Phase 10 features actually compose. Design in [`docs/PHASE10_1_DESIGN.md`](docs/PHASE10_1_DESIGN.md).

**MOS TCP listener (`src-tauri/src/mos.rs`, ~270 new lines):** tokio TCP listener spawns at Tauri `.setup()` time driven by persisted `mos_settings.json`'s `enabled` flag. Per connection: null-terminated-frame reader over `AsyncReadExt` with a 128 KB frame guard against a malformed message that never terminates; each frame parses through `parse_mos_message`; heartbeats ACK inline via `build_heartbeat_ack` (real MOS-wire `\x00` terminator, matching messageID); mosID handshake rejects connections whose `ncs_id` doesn't match the operator-configured `expected_ncs_id` (defense against accidental cross-connect to the wrong newsroom). Non-heartbeat messages emit a `mos:message` Tauri event with a `MosMessageEvent { role, message_id, ro_id, data }` payload for the Control Room. Two live rate/count caps: max 100 messages/sec per connection (rolling-window primitive matching the automation engine's), max 4 concurrent connections (a MOS listener isn't a public service). Live-config swap is via a new `restart_mos_server` Tauri command — the operator saves settings, clicks "Restart server" in the MOS strip; the accept-loop's `Notify` fires, all live connections drop cleanly, and a new listener spawns with the new config. Deliberate: hot-reload without an operator-visible action would rebind a port mid-broadcast.

**JS-side MOS consumer (`controlBridge.ts`):** `listen("mos:message", ...)` routes every parsed message into `usePlayoutStore` mutations via new pure helpers in `playout.ts`: `applyMosStoryDelete`, `applyMosStoryInsert` (target-id-aware — inserts before target or appends), `applyMosStoryMove` (moved items land in the ORDER specified by the wire, not their original position — matches MOS 2.8.5 spec semantics; a subtle bug my first implementation had, caught by verify-phase10_1), `applyMosStorySend` (updates by externalId, or adds if new). `roDelete` clears the entire rundown. All operations go through `replaceRundown` so Phase 7's ghost-`currentId` guard still fires on rundown-scale changes. `ProgramItem` gains a discriminated `externalId?: string` field (`mos:STORY01`, scoped so a future Rundown Studio externalId can coexist without collision) — persisted verbatim in localStorage, unaffected by CSV export.

**Automation composability (`automation.ts`):** `AutomationRule.action` → `AutomationRule.actions: AutomationAction[]` (breaking shape change with a real backward-compat path). `migrateRule()` promotes v1 `{action: X}` blobs to v2 `{actions: [X]}` on load; localStorage key stays `automation-rules-v1` (in-memory migration, persists as v2 on next save). Rate limiter accounting is **per action, not per rule** — a 3-action rule firing at 3/sec is 9 actions/sec against the 10/sec cap, which is exactly the runaway-rule shape the limiter exists to catch. New trigger kind `{ kind: "on_mos_message", roleFilter?: string }`: fires when the Rust listener emits a `mos:message` event; optional `roleFilter` restricts to one MOS message type (`"roCreate"`, `"roStorySend"`, etc.). Condition-field whitelist extends with two synthetic fields — `mosRole` and `mosRoId` — so a rule can gate on which rundown just changed:

```
trigger: on_mos_message with roleFilter="roCreate"
condition: mosRoId == "RO001"
actions: [ take, startRecord ]
```

**AutomationPanel gains a multi-action editor:** action list with per-action type dropdown + JSON params textarea + remove button (disabled when the rule has exactly one action — enforced by `validateRule`'s "must have at least one action" check). Trigger dropdown now includes `on_mos_message` with a role-filter input beside it. PlayoutPanel's MOS strip gains a **Restart server** button; the config dialog's Save button surfaces "server did not start" as an inline error when the port is already bound or blocked.

**Verified (2026-07-11):**
- `bunx tsc --noEmit` exit 0
- `bun run build` succeeds (20s)
- `bun run scripts/verify-phase10_1.ts` — **27/27 pass** covering MOS story mapping (slug/title fallback, duration defaults, non-positive-clamp guard), all 4 mutation ops (delete/insert-with-and-without-target/move-preserving-wire-order/send), multi-action validation (empty-rejected/unknown-in-array-rejected), on_mos_message trigger + condition-field whitelist, v1→v2 migration (single-action promotion, v2 preserved, non-object rejected), multi-action rate accounting (a 3-action rule at 4 fires trips the cap correctly)
- All previous verify scripts still pass (verify-phase10 shape updated for multi-action)
- `cargo test --lib` — **30/30 pass** (parser unchanged from Phase 10 — Rust unit tests still exercise the same synthetic XML samples; the TCP listener path is exercised end-to-end via the JS mutation ops)
- CI extended to run verify-phase10_1

**Two real bugs caught by verification writing:**
1. `applyMosStoryMove` originally preserved *original* order for the moved items, not the *wire order* specified in the message. MOS 2.8.5 spec says the message ordering is the operator's intent. Fixed.
2. Zero/negative `durationSec` originally clamped to 1s (matching the Rundown Studio Phase 9 policy). But MOS's `<storyDuration>` is genuinely optional — 0 or absent both mean "unknown". Fixed to fall back to the 30s default the Rust parser uses when absent. Two different systems, two different valid interpretations, documented inline.

**Not verified:** live NRCS interop (still needs iNews/ENPS/Octopus); live TCP listener under real network conditions. Recommend an operator pass — configure MOS, click Restart, run `nc localhost 10540 < roCreate_sample.xml` from a shell, watch items appear in the rundown tab.

**Explicit deferrals (Phase 10.2):**
- MOS outbound: BGE-to-NCS `roReq` / `roStorySchedule` (BGE reports status back).
- MOS `mosObj*` asset sync (still separate).
- Composable conditions (AND/OR chains) — multi-action gives operators the most-common ask; complex conditions can wait for a real use case.

## Phase 11+ — Spout/Syphon, signed installers, live profiling

---

## UI quality bar

The broadcast-console visual direction (near-black navy, blue accent, mono/Geist type) stays; Phase 0's token-centralization (`src/index.css`) is the enforcement mechanism so every new panel in every later phase inherits a consistent, professional look by construction — never a step down from the current visual ambition.
